diff --git a/src/d3d11/d3d11_annotation.h b/src/d3d11/d3d11_annotation.h
index 06d80af..f66818d 100644
--- a/src/d3d11/d3d11_annotation.h
+++ b/src/d3d11/d3d11_annotation.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_include.h"
 #include "../dxvk/dxvk_annotation.h"
 
diff --git a/src/d3d11/d3d11_buffer.h b/src/d3d11/d3d11_buffer.h
index c0472fd..cc12776 100644
--- a/src/d3d11/d3d11_buffer.h
+++ b/src/d3d11/d3d11_buffer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_cs.h"
 #include "../dxvk/dxvk_device.h"
 
diff --git a/src/d3d11/d3d11_cmd.h b/src/d3d11/d3d11_cmd.h
index 52f2f4d..504ac48 100644
--- a/src/d3d11/d3d11_cmd.h
+++ b/src/d3d11/d3d11_cmd.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_include.h"
 
 namespace dxvk {
diff --git a/src/d3d11/d3d11_cmdlist.h b/src/d3d11/d3d11_cmdlist.h
index 8be313a..fd5a06d 100644
--- a/src/d3d11/d3d11_cmdlist.h
+++ b/src/d3d11/d3d11_cmdlist.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_context.h"
 
 namespace dxvk {
diff --git a/src/d3d11/d3d11_context.h b/src/d3d11/d3d11_context.h
index 3bd60a5..2de5366 100644
--- a/src/d3d11/d3d11_context.h
+++ b/src/d3d11/d3d11_context.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_adapter.h"
 #include "../dxvk/dxvk_cs.h"
 #include "../dxvk/dxvk_device.h"
diff --git a/src/d3d11/d3d11_context_ext.h b/src/d3d11/d3d11_context_ext.h
index 2109a0d..5e5f63d 100644
--- a/src/d3d11/d3d11_context_ext.h
+++ b/src/d3d11/d3d11_context_ext.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_interfaces.h"
 
 namespace dxvk {
diff --git a/src/d3d11/d3d11_context_imm.h b/src/d3d11/d3d11_context_imm.h
index 9a5ba21..759626b 100644
--- a/src/d3d11/d3d11_context_imm.h
+++ b/src/d3d11/d3d11_context_imm.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/util_time.h"
 
 #include "../util/sync/sync_signal.h"
diff --git a/src/d3d11/d3d11_context_state.h b/src/d3d11/d3d11_context_state.h
index eca2a3c..7b99a2e 100644
--- a/src/d3d11/d3d11_context_state.h
+++ b/src/d3d11/d3d11_context_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <array>
 
 #include "d3d11_buffer.h"
diff --git a/src/d3d11/d3d11_cuda.h b/src/d3d11/d3d11_cuda.h
index a9fcdf4..1002331 100644
--- a/src/d3d11/d3d11_cuda.h
+++ b/src/d3d11/d3d11_cuda.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <utility>
 #include <vector>
 
diff --git a/src/d3d11/d3d11_device.h b/src/d3d11/d3d11_device.h
index 5a99bd6..2e09c0b 100644
--- a/src/d3d11/d3d11_device.h
+++ b/src/d3d11/d3d11_device.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 
diff --git a/src/d3d11/d3d11_device_child.h b/src/d3d11/d3d11_device_child.h
index 60eff07..20f63cd 100644
--- a/src/d3d11/d3d11_device_child.h
+++ b/src/d3d11/d3d11_device_child.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_include.h"
 
 #include "../util/com/com_private_data.h"
diff --git a/src/d3d11/d3d11_gdi.h b/src/d3d11/d3d11_gdi.h
index a2656c0..90b2eed 100644
--- a/src/d3d11/d3d11_gdi.h
+++ b/src/d3d11/d3d11_gdi.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "d3d11_include.h"
diff --git a/src/d3d11/d3d11_initializer.h b/src/d3d11/d3d11_initializer.h
index 8c96bab..fa453e9 100644
--- a/src/d3d11/d3d11_initializer.h
+++ b/src/d3d11/d3d11_initializer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_buffer.h"
 #include "d3d11_texture.h"
 
diff --git a/src/d3d11/d3d11_input_layout.h b/src/d3d11/d3d11_input_layout.h
index 8e472e2..16d6ecd 100644
--- a/src/d3d11/d3d11_input_layout.h
+++ b/src/d3d11/d3d11_input_layout.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_device_child.h"
 
 #include "../d3d10/d3d10_input_layout.h"
diff --git a/src/d3d11/d3d11_interfaces.h b/src/d3d11/d3d11_interfaces.h
index 3b0629b..7e217ec 100644
--- a/src/d3d11/d3d11_interfaces.h
+++ b/src/d3d11/d3d11_interfaces.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxgi/dxgi_interfaces.h"
 
 #include "d3d11_include.h"
diff --git a/src/d3d11/d3d11_interop.h b/src/d3d11/d3d11_interop.h
index 48ee4c3..bd71251 100644
--- a/src/d3d11/d3d11_interop.h
+++ b/src/d3d11/d3d11_interop.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxgi/dxgi_interfaces.h"
 
 #include "d3d11_include.h"
diff --git a/src/d3d11/d3d11_options.h b/src/d3d11/d3d11_options.h
index 99353b3..c496649 100644
--- a/src/d3d11/d3d11_options.h
+++ b/src/d3d11/d3d11_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/config/config.h"
 
 #include "../dxgi/dxgi_options.h"
diff --git a/src/d3d11/d3d11_query.h b/src/d3d11/d3d11_query.h
index a1853cf..cd17679 100644
--- a/src/d3d11/d3d11_query.h
+++ b/src/d3d11/d3d11_query.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_gpu_event.h"
 #include "../dxvk/dxvk_gpu_query.h"
 
diff --git a/src/d3d11/d3d11_sampler.h b/src/d3d11/d3d11_sampler.h
index 57fa139..971876f 100644
--- a/src/d3d11/d3d11_sampler.h
+++ b/src/d3d11/d3d11_sampler.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 
 #include "../d3d10/d3d10_sampler.h"
diff --git a/src/d3d11/d3d11_shader.h b/src/d3d11/d3d11_shader.h
index 3ab1685..c069936 100644
--- a/src/d3d11/d3d11_shader.h
+++ b/src/d3d11/d3d11_shader.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/d3d11/d3d11_state.h b/src/d3d11/d3d11_state.h
index e807d1a..a334b25 100644
--- a/src/d3d11/d3d11_state.h
+++ b/src/d3d11/d3d11_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <unordered_map>
 
 #include "d3d11_blend.h"
diff --git a/src/d3d11/d3d11_swapchain.h b/src/d3d11/d3d11_swapchain.h
index f4debea..a66243b 100644
--- a/src/d3d11/d3d11_swapchain.h
+++ b/src/d3d11/d3d11_swapchain.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_texture.h"
 
 #include "../dxvk/hud/dxvk_hud.h"
diff --git a/src/d3d11/d3d11_texture.h b/src/d3d11/d3d11_texture.h
index 2db2bc0..b25a72f 100644
--- a/src/d3d11/d3d11_texture.h
+++ b/src/d3d11/d3d11_texture.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_cs.h"
 #include "../dxvk/dxvk_device.h"
 
diff --git a/src/d3d11/d3d11_util.h b/src/d3d11/d3d11_util.h
index 749dc8b..797aeff 100644
--- a/src/d3d11/d3d11_util.h
+++ b/src/d3d11/d3d11_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 
 #include "../dxbc/dxbc_util.h"
diff --git a/src/d3d11/d3d11_video.h b/src/d3d11/d3d11_video.h
index 85979eb..9f64f77 100644
--- a/src/d3d11/d3d11_video.h
+++ b/src/d3d11/d3d11_video.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_device.h"
 
 namespace dxvk {
diff --git a/src/d3d11/d3d11_view.h b/src/d3d11/d3d11_view.h
index 50fcef1..90cbdfd 100644
--- a/src/d3d11/d3d11_view.h
+++ b/src/d3d11/d3d11_view.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d11_include.h"
 
 namespace dxvk {
diff --git a/src/d3d9/d3d9_annotation.h b/src/d3d9/d3d9_annotation.h
index 1fdb19f..d34120a 100644
--- a/src/d3d9/d3d9_annotation.h
+++ b/src/d3d9/d3d9_annotation.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device.h"
 #include "d3d9_include.h"
 #include "../dxvk/dxvk_annotation.h"
diff --git a/src/d3d9/d3d9_caps.h b/src/d3d9/d3d9_caps.h
index a894de1..43fb8c9 100644
--- a/src/d3d9/d3d9_caps.h
+++ b/src/d3d9/d3d9_caps.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 namespace dxvk::caps {
diff --git a/src/d3d9/d3d9_common_buffer.h b/src/d3d9/d3d9_common_buffer.h
index c3bc413..ba918ae 100644
--- a/src/d3d9/d3d9_common_buffer.h
+++ b/src/d3d9/d3d9_common_buffer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 #include "../dxvk/dxvk_cs.h"
 
diff --git a/src/d3d9/d3d9_common_texture.h b/src/d3d9/d3d9_common_texture.h
index e52ead2..a40f8d7 100644
--- a/src/d3d9/d3d9_common_texture.h
+++ b/src/d3d9/d3d9_common_texture.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_format.h"
 #include "d3d9_util.h"
 #include "d3d9_caps.h"
@@ -443,6 +446,26 @@ namespace dxvk {
      * \param [in] Subresource Subresource index
      * \param [in] Seq Sequence number
      */
+    /**
+     * \brief Early read-back fence (D3D9_RT_EARLY_FENCE)
+     *
+     * When the small visibility render target is unbound, the copy into its staging
+     * buffer is emitted and flushed immediately, and the submission carrying it is
+     * remembered here. The next Lock waits on that submission's fence directly and
+     * skips emitting a second copy. Consumed (cleared) by the lock.
+     */
+    void SetEarlyReadback(Rc<DxvkCommandList> List) {
+      m_earlyReadback = std::move(List);
+    }
+
+    Rc<DxvkCommandList> TakeEarlyReadback() {
+      return std::exchange(m_earlyReadback, nullptr);
+    }
+
+    bool HasEarlyReadback() const {
+      return m_earlyReadback != nullptr;
+    }
+
     void TrackMappingBufferSequenceNumber(UINT Subresource, uint64_t Seq) {
       if (Subresource < m_seqs.size())
         m_seqs[Subresource] = Seq;
@@ -477,6 +500,8 @@ namespace dxvk {
     D3D9SubresourceArray<
       uint64_t>                   m_seqs = { };
 
+    Rc<DxvkCommandList>           m_earlyReadback;
+
     D3D9_VK_FORMAT_MAPPING        m_mapping;
 
     bool                          m_shadow; //< Depth Compare-ness
diff --git a/src/d3d9/d3d9_cursor.h b/src/d3d9/d3d9_cursor.h
index 32645d2..f729b8f 100644
--- a/src/d3d9/d3d9_cursor.h
+++ b/src/d3d9/d3d9_cursor.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 namespace dxvk {
diff --git a/src/d3d9/d3d9_device.cpp b/src/d3d9/d3d9_device.cpp
index 734a7a0..db7a47c 100644
--- a/src/d3d9/d3d9_device.cpp
+++ b/src/d3d9/d3d9_device.cpp
@@ -1,5 +1,8 @@
 #include "d3d9_device.h"
 
+#include <mutex>
+#include <unordered_map>
+
 #include "d3d9_annotation.h"
 #include "d3d9_interface.h"
 #include "d3d9_swapchain.h"
@@ -30,8 +33,353 @@
 #pragma fenv_access (on)
 #endif
 
+#include <set>
+#include <cstring>
+#include <cstdio>
+#include <cstdlib>
+#include <chrono>
+#include <map>
+#include <string>
+
+namespace {
+  // Batching-headroom probe (DXVK_BATCH_PROBE=1). FFXI submits thousands of tiny draws per
+  // frame and the stack is bound by draw submission cost, not by the GPU. The question that
+  // decides whether an instancing/merging layer is worth building is: how many of those draws
+  // actually share pipeline state and textures? If N draws collapse into a handful of state
+  // groups, merging them is a large win. If nearly every draw is its own group, it is not.
+  struct BatchProbe {
+    uint64_t draws = 0;
+    std::set<uint64_t> groups;         // distinct (shaders, textures, decl) signatures
+    std::set<uint64_t> texGroups;      // distinct texture-0 bindings alone
+    std::set<uint64_t> fullGroups;     // signature + world transform + FF render states
+    std::set<uint64_t> meshGroups;     // signature + the actual mesh (vertex+index buffer)
+    uint64_t instanceable = 0;         // draws that repeat a mesh already seen this frame
+    uint64_t lastMesh = 0;             // previous draw's mesh+state, for run detection
+    uint64_t runMerges = 0;            // draws that continue the immediately preceding run
+    uint64_t opaqueRunMerges = 0;      // same, but only where alpha blending is off
+    bool     lastBlend = false;
+    uint64_t blendEnabled = 0;         // draws with ALPHABLENDENABLE set
+    uint64_t blendIsOpaque = 0;        // ...of those, ones whose factors are ONE/ZERO
+    uint64_t effOpaqueRun = 0;         // consecutive repeats that are *effectively* opaque
+    uint64_t frames = 0;
+    FILE* out = nullptr;
+    bool enabled = std::getenv("DXVK_BATCH_PROBE") != nullptr;
+    void note(uint64_t sig, uint64_t tex, uint64_t full, uint64_t mesh, bool blend, bool opaqueFactors) {
+      if (!enabled) return;
+      draws++; groups.insert(sig); texGroups.insert(tex); fullGroups.insert(full);
+      if (!meshGroups.insert(mesh).second) instanceable++;   // this mesh+state seen before
+      // A *consecutive* repeat is what a simple batcher can capture without reordering draws.
+      if (blend) { blendEnabled++; if (opaqueFactors) blendIsOpaque++; }
+      // "Effectively opaque" = blending off, or on but with ONE/ZERO factors, which writes the
+      // source colour straight out. Those are order-independent and safe to reorder/batch.
+      const bool effOpaque = !blend || opaqueFactors;
+      if (draws > 1 && mesh == lastMesh) {
+        runMerges++;
+        if (!blend && !lastBlend) opaqueRunMerges++;
+        if (effOpaque) effOpaqueRun++;
+      }
+      lastMesh = mesh; lastBlend = blend;
+    }
+    void frame() {
+      if (!enabled) return;
+      frames++;
+      if (draws == 0) return;
+      if (!out) out = fopen("/tmp/dxvk-batch-probe.log", "w");
+      if (out && (frames % 30) == 0) {
+        fprintf(out, "frame %llu draws %llu stategroups %zu texgroups %zu fullgroups %zu meshgroups %zu instanceable %llu runmerges %llu opaquerun %llu blend %llu blendopaque %llu effopaquerun %llu  merge_ratio %.1fx  trivial_merge %.1fx  instancing_ratio %.1fx\n",
+                (unsigned long long)frames, (unsigned long long)draws,
+                groups.size(), texGroups.size(), fullGroups.size(), meshGroups.size(),
+                (unsigned long long)instanceable,
+                (unsigned long long)runMerges, (unsigned long long)opaqueRunMerges,
+                (unsigned long long)blendEnabled, (unsigned long long)blendIsOpaque,
+                (unsigned long long)effOpaqueRun,
+                groups.empty() ? 0.0 : double(draws) / double(groups.size()),
+                fullGroups.empty() ? 0.0 : double(draws) / double(fullGroups.size()),
+                meshGroups.empty() ? 0.0 : double(draws) / double(meshGroups.size()));
+        fflush(out);
+      }
+      draws = 0; instanceable = 0; runMerges = 0; opaqueRunMerges = 0; blendEnabled = 0; blendIsOpaque = 0; effOpaqueRun = 0; lastMesh = 0; groups.clear(); texGroups.clear(); fullGroups.clear(); meshGroups.clear();
+    }
+  };
+  static BatchProbe g_batchProbe;
+}
+
 namespace dxvk {
 
+namespace {
+  // Flush attribution (DXVK_FLUSH_LOG=<path>).
+  //
+  // In-world FFXI submits ~33 command buffers per frame; character select, at a comparable draw
+  // count, submits ~6. Each submit is a command-buffer commit on Metal, so knowing *which*
+  // D3D9 entry point is forcing them is the difference between guessing and fixing.
+  enum FlushSite { kFsUpdateTex, kFsSetRT, kFsSetDS, kFsEndScene, kFsMisc1, kFsMisc2,
+                   kFsQueryEnd, kFsNumSites };
+  static const char* kFlushSiteNames[kFsNumSites] = {
+    "UpdateTexture", "SetRenderTarget", "SetDepthStencil", "EndScene",
+    "misc4475", "misc5158", "QueryEnd" };
+
+  struct FlushProbe {
+    FILE* file = nullptr;
+    uint64_t calls[kFsNumSites] = { };
+    uint64_t flushes[kFsNumSites] = { };
+    std::chrono::steady_clock::time_point tick;
+    uint64_t frames = 0;
+
+    FlushProbe() {
+      const char* p = std::getenv("DXVK_FLUSH_LOG");
+      if (p && *p) file = std::fopen(p, "a");
+      if (file) {
+        std::fprintf(file, "fps");
+        for (size_t i = 0; i < kFsNumSites; i++)
+          std::fprintf(file, ",%s_calls,%s_flushes", kFlushSiteNames[i], kFlushSiteNames[i]);
+        std::fprintf(file, "\n");
+        std::fflush(file);
+      }
+      tick = std::chrono::steady_clock::now();
+    }
+    bool on() const { return file != nullptr; }
+    void call(FlushSite s) { if (file) calls[s]++; }
+    void flushed(FlushSite s) { if (file) flushes[s]++; }
+    void frame() {
+      if (!file) return;
+      frames++;
+      auto now = std::chrono::steady_clock::now();
+      double dt = std::chrono::duration<double>(now - tick).count();
+      if (dt < 1.0) return;
+      std::fprintf(file, "%.2f", double(frames) / dt);
+      for (size_t i = 0; i < kFsNumSites; i++) {
+        std::fprintf(file, ",%.1f,%.1f", double(calls[i]) / frames, double(flushes[i]) / frames);
+        calls[i] = 0; flushes[i] = 0;
+      }
+      std::fprintf(file, "\n");
+      std::fflush(file);
+      tick = now; frames = 0;
+    }
+  };
+  static FlushProbe g_flushProbe;
+  static FlushSite g_currentSite = kFsMisc1;
+
+  // Stall attribution (DXVK_STALL_LOG=<path>).
+  //
+  // The 4K frame is 40 ms with the GPU doing ~3 ms of work and neither of the two busy threads
+  // saturated -- the profile of two threads waiting on each other rather than of either being
+  // the cost. This measures the waiting directly: how long the calling thread spends blocked
+  // inside the places where D3D9 can block, per frame. Reported in milliseconds per frame, so
+  // a bucket at 20 in a 40 ms frame is half the frame spent doing nothing.
+  //
+  // Deliberately cheap: two clock reads per instrumented call and no allocation, unlike
+  // DXVK_DRAW_PROBE, which crashes the client at 4K with the sidecar attached.
+  enum StallSite { kStMapWait, kStMapBlocked, kStSyncCs, kStNumSites };
+  static const char* kStallSiteNames[kStNumSites] = { "map_wait", "map_blocked", "sync_cs" };
+
+  struct StallProbe {
+    FILE* file = nullptr;
+    uint64_t ns[kStNumSites] = { };
+    uint64_t hits[kStNumSites] = { };
+    std::chrono::steady_clock::time_point tick;
+    uint64_t frames = 0;
+
+    StallProbe() {
+      const char* p = std::getenv("DXVK_STALL_LOG");
+      if (p && *p) file = std::fopen(p, "a");
+      if (file) {
+        std::fprintf(file, "fps");
+        for (size_t i = 0; i < kStNumSites; i++)
+          std::fprintf(file, ",%s_ms,%s_n", kStallSiteNames[i], kStallSiteNames[i]);
+        std::fprintf(file, "\n");
+        std::fflush(file);
+      }
+      tick = std::chrono::steady_clock::now();
+    }
+    bool on() const { return file != nullptr; }
+    void add(StallSite s, std::chrono::steady_clock::time_point t0) {
+      if (!file) return;
+      ns[s] += std::chrono::duration_cast<std::chrono::nanoseconds>(
+        std::chrono::steady_clock::now() - t0).count();
+      hits[s]++;
+    }
+    void frame() {
+      if (!file) return;
+      frames++;
+      auto now = std::chrono::steady_clock::now();
+      double dt = std::chrono::duration<double>(now - tick).count();
+      if (dt < 1.0) return;
+      std::fprintf(file, "%.2f", double(frames) / dt);
+      for (size_t i = 0; i < kStNumSites; i++) {
+        std::fprintf(file, ",%.2f,%.1f",
+                     double(ns[i]) / 1.0e6 / double(frames), double(hits[i]) / double(frames));
+        ns[i] = 0; hits[i] = 0;
+      }
+      std::fprintf(file, "\n");
+      std::fflush(file);
+      tick = now; frames = 0;
+    }
+  };
+  static StallProbe g_stallProbe;
+}
+
+
+
+  // Draw-cost probe (DXVK_DRAW_PROBE=<path>). Reports, once per second of wall time, how many
+  // milliseconds of that second were spent inside our D3D9 draw entry points. Compared against
+  // the present probe's "outside Present" figure this says whether a slow frame is our
+  // translation cost or the game's own work.
+  namespace {
+    // Counts how many D3D9 API calls the game makes per second, by kind. FFXI is a 2002
+    // immediate-mode engine: if the per-call overhead through Ashita -> d3d8to9 -> DXVK is what
+    // costs the frame, the call volume is where it will show.
+    enum D3D9ApiCall {
+      ApiSetRenderState, ApiSetTexture, ApiSetTextureStageState, ApiSetSamplerState,
+      ApiSetTransform, ApiSetStreamSource, ApiSetIndices, ApiSetVertexShaderConstantF,
+      ApiSetPixelShaderConstantF, ApiSetVertexShader, ApiSetPixelShader,
+      ApiSetVertexDeclaration, ApiSetFVF, ApiClear, ApiSetViewport,
+      ApiLockBuffer, ApiLockImage, ApiCount
+    };
+    static const char* g_apiNames[ApiCount] = {
+      "SetRenderState", "SetTexture", "SetTextureStageState", "SetSamplerState",
+      "SetTransform", "SetStreamSource", "SetIndices", "SetVertexShaderConstantF",
+      "SetPixelShaderConstantF", "SetVertexShader", "SetPixelShader",
+      "SetVertexDeclaration", "SetFVF", "Clear", "SetViewport",
+      "LockBuffer", "LockImage"
+    };
+
+    struct D3D9DrawProbe {
+      FILE* file = nullptr;
+      std::chrono::steady_clock::time_point tick;
+      double drawMs = 0.0;
+      double apiMs = 0.0;
+      uint64_t draws = 0;
+      uint64_t api[ApiCount] = { };
+
+      D3D9DrawProbe() {
+        const char* p = std::getenv("DXVK_DRAW_PROBE");
+        if (p && *p)
+          file = std::fopen(p, "a");
+        if (file)
+        {
+          std::fprintf(file, "draws_per_sec,draw_ms_per_sec,api_ms_per_sec");
+          for (int i = 0; i < ApiCount; i++)
+            std::fprintf(file, ",%s", g_apiNames[i]);
+          for (size_t i = 0; i < NumGapBuckets; i++)
+            std::fprintf(file, ",gap%zu_n,gap%zu_ms", i, i);
+          for (int i = 0; i < ApiCount; i++)
+            std::fprintf(file, ",before_%s_n,before_%s_ms", g_apiNames[i], g_apiNames[i]);
+          std::fprintf(file, "\n");
+        }
+        tick = std::chrono::steady_clock::now();
+      }
+
+      inline bool on() const { return file != nullptr; }
+
+      inline void count(D3D9ApiCall which) { if (file) api[which] += 1; }
+
+      // Time between leaving one D3D9 entry point and entering the next. That interval is
+      // everything above us: FFXI's own code plus the Ashita and d3d8to9 proxies. Bucketing it
+      // separates a uniform per-call tax (thousands of ~1us gaps) from the game doing real work
+      // (a few very large gaps). Buckets are upper bounds in microseconds.
+      static constexpr size_t NumGapBuckets = 7;
+      static constexpr double GapEdges[NumGapBuckets] = { 1, 2, 5, 10, 50, 500, 1e9 };
+      uint64_t gapCount[NumGapBuckets] = { };
+      double   gapMs[NumGapBuckets] = { };
+      std::chrono::steady_clock::time_point lastExit;
+      bool haveLastExit = false;
+
+      // Which call the game makes *after* a long pause says what it was busy doing: a pause
+      // before Clear is the frame boundary, before LockBuffer it just built geometry on the CPU.
+      uint64_t bigGapBy[ApiCount] = { };
+      double   bigGapMsBy[ApiCount] = { };
+
+      inline void enterFrom(D3D9ApiCall which, std::chrono::steady_clock::time_point t0) {
+        if (!haveLastExit)
+          return;
+        double us = std::chrono::duration<double, std::micro>(t0 - lastExit).count();
+        for (size_t i = 0; i < NumGapBuckets; i++) {
+          if (us <= GapEdges[i]) {
+            gapCount[i] += 1;
+            gapMs[i] += us / 1000.0;
+            break;
+          }
+        }
+        if (us > 100.0) {
+          bigGapBy[which] += 1;
+          bigGapMsBy[which] += us / 1000.0;
+        }
+      }
+
+      inline void addApi(D3D9ApiCall which, std::chrono::steady_clock::time_point t0) {
+        api[which] += 1;
+        auto now = std::chrono::steady_clock::now();
+        apiMs += std::chrono::duration<double, std::milli>(now - t0).count();
+        lastExit = now;
+        haveLastExit = true;
+      }
+
+      void add(std::chrono::steady_clock::time_point t0) {
+        auto now = std::chrono::steady_clock::now();
+        drawMs += std::chrono::duration<double, std::milli>(now - t0).count();
+        draws += 1;
+        double dt = std::chrono::duration<double>(now - tick).count();
+        if (dt < 1.0)
+          return;
+        std::fprintf(file, "%.0f,%.1f,%.1f", double(draws) / dt, drawMs / dt, apiMs / dt);
+        apiMs = 0.0;
+        for (int i = 0; i < ApiCount; i++) {
+          std::fprintf(file, ",%.0f", double(api[i]) / dt);
+          api[i] = 0;
+        }
+        for (size_t i = 0; i < NumGapBuckets; i++) {
+          std::fprintf(file, ",%.0f,%.1f", double(gapCount[i]) / dt, gapMs[i] / dt);
+          gapCount[i] = 0;
+          gapMs[i] = 0.0;
+        }
+        for (int i = 0; i < ApiCount; i++) {
+          std::fprintf(file, ",%.0f,%.1f", double(bigGapBy[i]) / dt, bigGapMsBy[i] / dt);
+          bigGapBy[i] = 0;
+          bigGapMsBy[i] = 0.0;
+        }
+        std::fprintf(file, "\n");
+        std::fflush(file);
+        tick = now;
+        draws = 0;
+        drawMs = 0.0;
+      }
+    };
+    static D3D9DrawProbe g_drawProbe;
+
+    struct D3D9ApiTimer {
+      std::chrono::steady_clock::time_point t0;
+      D3D9ApiCall which;
+      bool on;
+      D3D9ApiTimer(D3D9ApiCall w) : which(w), on(g_drawProbe.on()) {
+        if (on) {
+          t0 = std::chrono::steady_clock::now();
+          g_drawProbe.enterFrom(w, t0);
+        }
+      }
+      ~D3D9ApiTimer() { if (on) g_drawProbe.addApi(which, t0); }
+    };
+
+    // Isolation switch (DXVK_SKIP_DRAWS=1): accept draw calls and do nothing with them. The
+    // picture is wrong -- this is a measurement tool, never a shipping option -- but the frame
+    // time it produces is the floor set by everything above us: FFXI's own per-frame work plus
+    // the Ashita and d3d8to9 wrappers. Compare it with the real frame time to see how much of
+    // the frame the renderer is actually responsible for.
+    static const bool g_skipDraws = [] {
+      const char* v = std::getenv("DXVK_SKIP_DRAWS");
+      return v != nullptr && *v == '1';
+    }();
+
+    struct D3D9DrawTimer {
+      std::chrono::steady_clock::time_point t0;
+      bool on;
+      D3D9DrawTimer() : on(g_drawProbe.on()) {
+        if (on) t0 = std::chrono::steady_clock::now();
+      }
+      ~D3D9DrawTimer() { if (on) g_drawProbe.add(t0); }
+    };
+  }
+
+
   D3D9DeviceEx::D3D9DeviceEx(
           D3D9InterfaceEx*       pParent,
           D3D9Adapter*           pAdapter,
@@ -376,6 +724,10 @@ namespace dxvk {
     const RECT*    pDestRect,
           HWND     hDestWindowOverride,
     const RGNDATA* pDirtyRegion) {
+    g_batchProbe.frame();
+    g_flushProbe.frame();
+    g_stallProbe.frame();
+
     return PresentEx(
       pSourceRect,
       pDestRect,
@@ -1237,6 +1589,98 @@ namespace dxvk {
       return D3DERR_INVALIDCALL;
 
     if (RenderTargetIndex == 0) {
+      // D3D9_RT_UNBIND_FLUSH=<max edge in px>: when the game stops drawing to a small render
+      // target (the visibility-test surface), submit everything recorded so far. By the time
+      // the game locks that surface a few calls later, the frame's draw submissions are
+      // already executing, so the lock's own flush is a micro-submission and its fence
+      // (D3D9_RT_READBACK_FENCE) signals almost immediately. Draw-order and results are
+      // unchanged — this only moves the submission boundary.
+      static const uint32_t rtUnbindFlushMaxEdge = [] {
+        const char* v = std::getenv("D3D9_RT_UNBIND_FLUSH");
+        return (v != nullptr && *v != '\0') ? uint32_t(std::atoi(v)) : 0u;
+      }();
+      if (rtUnbindFlushMaxEdge != 0) {
+        auto* prev = m_state.renderTargets[0].ptr();
+        if (prev != nullptr && prev != rt) {
+          auto* prevTex = prev->GetCommonTexture();
+          if (prevTex != nullptr) {
+            const auto* pd = prevTex->Desc();
+            if (pd->Pool == D3DPOOL_DEFAULT && (pd->Usage & D3DUSAGE_RENDERTARGET)
+             && pd->Width <= rtUnbindFlushMaxEdge && pd->Height <= rtUnbindFlushMaxEdge)
+              Flush();
+          }
+        }
+      }
+      // D3D9_RT_EARLY_FENCE=<max edge in px>: when the visibility render target is
+      // unbound, emit its image->staging copy NOW and flush, so the copy rides in the
+      // same submission as the pass that produced it (pending depth 1 at the lock,
+      // ~0.7 ms) instead of a second lock-time submission behind it (depth 2, ~2 ms —
+      // fence-depth probe, 2026-08-20). The submission is remembered on the texture;
+      // the next Lock waits its fence and skips emitting a second copy. Bytes are
+      // identical to the slow path: the pattern is strictly render -> unbind -> lock
+      // (54k-event interleave probe), nothing touches the surface in between.
+      static const uint32_t rtEarlyFenceMaxEdge = [] {
+        const char* v = std::getenv("D3D9_RT_EARLY_FENCE");
+        return (v != nullptr && *v != '\0') ? uint32_t(std::atoi(v)) : 0u;
+      }();
+      if (rtEarlyFenceMaxEdge != 0) {
+        auto* prev = m_state.renderTargets[0].ptr();
+        if (prev != nullptr && prev != rt) {
+          auto* prevTex = prev->GetCommonTexture();
+          if (prevTex != nullptr) {
+            const auto* pd = prevTex->Desc();
+            Rc<DxvkImage>  img = prevTex->GetImage();
+            Rc<DxvkBuffer> buf = prevTex->GetBuffer(0);
+            if (pd->Pool == D3DPOOL_DEFAULT && (pd->Usage & D3DUSAGE_RENDERTARGET)
+             && pd->Width <= rtEarlyFenceMaxEdge && pd->Height <= rtEarlyFenceMaxEdge
+             && img != nullptr && buf != nullptr
+             && img->info().sampleCount == VK_SAMPLE_COUNT_1_BIT
+             && !IsDepthFormat(pd->Format)) {
+              auto subres = prevTex->GetSubresourceFromIndex(VK_IMAGE_ASPECT_COLOR_BIT, 0);
+              auto layers = vk::makeSubresourceLayers(subres);
+              VkExtent3D extent = img->mipLevelExtent(0);
+              EmitCs([
+                cImageBuffer = buf,
+                cImage       = img,
+                cSubres      = layers,
+                cExtent      = extent
+              ] (DxvkContext* ctx) {
+                ctx->copyImageToBuffer(cImageBuffer, 0, 4, 0,
+                  cImage, cSubres, VkOffset3D { 0, 0, 0 }, cExtent);
+              });
+              TrackTextureMappingBufferSequenceNumber(prevTex, 0);
+              Flush();
+              // The CS thread must have processed the flush before the submission can be
+              // captured; an earlier capture waits an older fence = stale bytes.
+              SynchronizeCsThread(DxvkCsThread::SynchronizeAll);
+              prevTex->SetEarlyReadback(m_dxvkDevice->lastSubmittedList());
+            }
+          }
+        }
+      }
+      // Companion to D3D9_RT_IDENT_PROBE: log when a small RT is bound ("B") or unbound
+      // ("U"), so lock lines ("L", written at LockImage) can be interleaved with render
+      // passes. Decides whether per-frame lock dedup is sound.
+      static FILE* rtIdentProbe2 = [] () -> FILE* {
+        const char* v = std::getenv("D3D9_RT_IDENT_PROBE");
+        return (v != nullptr && *v != '\0') ? std::fopen(v, "a") : nullptr;
+      }();
+      if (rtIdentProbe2 != nullptr) {
+        auto logEvt = [&] (char kind, D3D9Surface* surf) {
+          if (surf == nullptr) return;
+          auto* t = surf->GetCommonTexture();
+          if (t == nullptr) return;
+          const auto* td = t->Desc();
+          if (td->Pool != D3DPOOL_DEFAULT || !(td->Usage & D3DUSAGE_RENDERTARGET)
+           || td->Width > 32 || td->Height > 32) return;
+          auto us = std::chrono::duration_cast<std::chrono::microseconds>(
+            std::chrono::steady_clock::now().time_since_epoch()).count();
+          std::fprintf(rtIdentProbe2, "%lld,%c,%p\n", (long long)us, kind, (void*)t);
+        };
+        logEvt('U', m_state.renderTargets[0].ptr());
+        logEvt('B', rt);
+      }
+
       auto rtSize = rt->GetSurfaceExtent();
 
       D3DVIEWPORT9 viewport;
@@ -1270,6 +1714,7 @@ namespace dxvk {
       return D3D_OK;
 
     // Do a strong flush if the first render target is changed.
+    g_currentSite = kFsSetRT;
     FlushImplicit(RenderTargetIndex == 0 ? TRUE : FALSE);
     m_flags.set(D3D9DeviceFlag::DirtyFramebuffer);
 
@@ -1390,6 +1835,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::EndScene() {
+    g_currentSite = kFsEndScene;
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(!m_flags.test(D3D9DeviceFlag::InScene)))
@@ -1410,6 +1856,7 @@ namespace dxvk {
           D3DCOLOR Color,
           float    Z,
           DWORD    Stencil) {
+    D3D9ApiTimer apiTimer(ApiClear);
     if (unlikely(!Count && pRects))
       return D3D_OK;
 
@@ -1569,6 +2016,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetTransform(D3DTRANSFORMSTATETYPE State, const D3DMATRIX* pMatrix) {
+    D3D9ApiTimer apiTimer(ApiSetTransform);
     return SetStateTransform(GetTransformIndex(State), pMatrix);
   }
 
@@ -1605,6 +2053,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetViewport(const D3DVIEWPORT9* pViewport) {
+    D3D9ApiTimer apiTimer(ApiSetViewport);
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(ShouldRecord()))
@@ -1787,6 +2236,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetRenderState(D3DRENDERSTATETYPE State, DWORD Value) {
+    D3D9ApiTimer apiTimer(ApiSetRenderState);
     D3D9DeviceLock lock = LockDevice();
 
     // D3D9 only allows reading for values 0 and 7-255 so we don't need to do anything but return OK
@@ -2226,6 +2676,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetTexture(DWORD Stage, IDirect3DBaseTexture9* pTexture) {
+    D3D9ApiTimer apiTimer(ApiSetTexture);
     if (unlikely(InvalidSampler(Stage)))
       return D3D_OK;
 
@@ -2261,6 +2712,7 @@ namespace dxvk {
           DWORD                    Stage,
           D3DTEXTURESTAGESTATETYPE Type,
           DWORD                    Value) {
+    D3D9ApiTimer apiTimer(ApiSetTextureStageState);
     return SetStateTextureStageState(Stage, RemapTextureStageStateType(Type), Value);
   }
 
@@ -2291,6 +2743,7 @@ namespace dxvk {
           DWORD               Sampler,
           D3DSAMPLERSTATETYPE Type,
           DWORD               Value) {
+    D3D9ApiTimer apiTimer(ApiSetSamplerState);
     if (unlikely(InvalidSampler(Sampler)))
       return D3D_OK;
 
@@ -2397,6 +2850,42 @@ namespace dxvk {
           D3DPRIMITIVETYPE PrimitiveType,
           UINT             StartVertex,
           UINT             PrimitiveCount) {
+    D3D9DrawTimer drawTimer;
+    if (g_skipDraws)
+      return D3D_OK;
+    if (g_batchProbe.enabled) {
+      uint64_t tex = reinterpret_cast<uint64_t>(m_state.textures[0]);
+      uint64_t sig = tex
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexShader.ptr())  * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.pixelShader.ptr())   * 0xC2B2AE3D27D4EB4Full)
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexDecl.ptr())    * 0x165667B19E3779F9ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.textures[1])         * 0x27D4EB2F165667C5ull);
+      uint64_t full = sig;
+      {
+        // fold in the world transform -- if this collapses as well, draws sharing state also
+        // share their transform and can be concatenated without any instancing work.
+        const auto& wm = m_state.transforms[GetTransformIndex(D3DTS_WORLD)];
+        const float* f = reinterpret_cast<const float*>(&wm);
+        for (int i = 0; i < 16; i++) {
+          uint32_t bits; std::memcpy(&bits, &f[i], sizeof(bits));
+          full = full * 0x100000001B3ull ^ bits;
+        }
+        // and the fixed-function render states the shader reads
+        for (auto rs : { D3DRS_ALPHABLENDENABLE, D3DRS_SRCBLEND, D3DRS_DESTBLEND,
+                         D3DRS_ALPHATESTENABLE, D3DRS_ALPHAREF, D3DRS_CULLMODE,
+                         D3DRS_ZWRITEENABLE, D3DRS_ZFUNC, D3DRS_FOGENABLE })
+          full = full * 0x100000001B3ull ^ uint64_t(m_state.renderStates[rs]);
+      }
+      // mesh identity: same state AND same geometry -> this is what instancing can collapse.
+      uint64_t mesh = sig
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexBuffers[0].vertexBuffer.ptr()) * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.indices.ptr())                       * 0xC2B2AE3D27D4EB4Full);
+      g_batchProbe.note(sig, tex, full, mesh,
+        m_state.renderStates[D3DRS_ALPHABLENDENABLE] != 0,
+        m_state.renderStates[D3DRS_SRCBLEND] == D3DBLEND_ONE &&
+        m_state.renderStates[D3DRS_DESTBLEND] == D3DBLEND_ZERO);
+    }
+
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(m_state.vertexDecl == nullptr))
@@ -2433,6 +2922,42 @@ namespace dxvk {
           UINT             NumVertices,
           UINT             StartIndex,
           UINT             PrimitiveCount) {
+    D3D9DrawTimer drawTimer;
+    if (g_skipDraws)
+      return D3D_OK;
+    if (g_batchProbe.enabled) {
+      uint64_t tex = reinterpret_cast<uint64_t>(m_state.textures[0]);
+      uint64_t sig = tex
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexShader.ptr())  * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.pixelShader.ptr())   * 0xC2B2AE3D27D4EB4Full)
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexDecl.ptr())    * 0x165667B19E3779F9ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.textures[1])         * 0x27D4EB2F165667C5ull);
+      uint64_t full = sig;
+      {
+        // fold in the world transform -- if this collapses as well, draws sharing state also
+        // share their transform and can be concatenated without any instancing work.
+        const auto& wm = m_state.transforms[GetTransformIndex(D3DTS_WORLD)];
+        const float* f = reinterpret_cast<const float*>(&wm);
+        for (int i = 0; i < 16; i++) {
+          uint32_t bits; std::memcpy(&bits, &f[i], sizeof(bits));
+          full = full * 0x100000001B3ull ^ bits;
+        }
+        // and the fixed-function render states the shader reads
+        for (auto rs : { D3DRS_ALPHABLENDENABLE, D3DRS_SRCBLEND, D3DRS_DESTBLEND,
+                         D3DRS_ALPHATESTENABLE, D3DRS_ALPHAREF, D3DRS_CULLMODE,
+                         D3DRS_ZWRITEENABLE, D3DRS_ZFUNC, D3DRS_FOGENABLE })
+          full = full * 0x100000001B3ull ^ uint64_t(m_state.renderStates[rs]);
+      }
+      // mesh identity: same state AND same geometry -> this is what instancing can collapse.
+      uint64_t mesh = sig
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexBuffers[0].vertexBuffer.ptr()) * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.indices.ptr())                       * 0xC2B2AE3D27D4EB4Full);
+      g_batchProbe.note(sig, tex, full, mesh,
+        m_state.renderStates[D3DRS_ALPHABLENDENABLE] != 0,
+        m_state.renderStates[D3DRS_SRCBLEND] == D3DBLEND_ONE &&
+        m_state.renderStates[D3DRS_DESTBLEND] == D3DBLEND_ZERO);
+    }
+
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(m_state.vertexDecl == nullptr))
@@ -2469,6 +2994,39 @@ namespace dxvk {
           UINT             PrimitiveCount,
     const void*            pVertexStreamZeroData,
           UINT             VertexStreamZeroStride) {
+    if (g_batchProbe.enabled) {
+      uint64_t tex = reinterpret_cast<uint64_t>(m_state.textures[0]);
+      uint64_t sig = tex
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexShader.ptr())  * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.pixelShader.ptr())   * 0xC2B2AE3D27D4EB4Full)
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexDecl.ptr())    * 0x165667B19E3779F9ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.textures[1])         * 0x27D4EB2F165667C5ull);
+      uint64_t full = sig;
+      {
+        // fold in the world transform -- if this collapses as well, draws sharing state also
+        // share their transform and can be concatenated without any instancing work.
+        const auto& wm = m_state.transforms[GetTransformIndex(D3DTS_WORLD)];
+        const float* f = reinterpret_cast<const float*>(&wm);
+        for (int i = 0; i < 16; i++) {
+          uint32_t bits; std::memcpy(&bits, &f[i], sizeof(bits));
+          full = full * 0x100000001B3ull ^ bits;
+        }
+        // and the fixed-function render states the shader reads
+        for (auto rs : { D3DRS_ALPHABLENDENABLE, D3DRS_SRCBLEND, D3DRS_DESTBLEND,
+                         D3DRS_ALPHATESTENABLE, D3DRS_ALPHAREF, D3DRS_CULLMODE,
+                         D3DRS_ZWRITEENABLE, D3DRS_ZFUNC, D3DRS_FOGENABLE })
+          full = full * 0x100000001B3ull ^ uint64_t(m_state.renderStates[rs]);
+      }
+      // mesh identity: same state AND same geometry -> this is what instancing can collapse.
+      uint64_t mesh = sig
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexBuffers[0].vertexBuffer.ptr()) * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.indices.ptr())                       * 0xC2B2AE3D27D4EB4Full);
+      g_batchProbe.note(sig, tex, full, mesh,
+        m_state.renderStates[D3DRS_ALPHABLENDENABLE] != 0,
+        m_state.renderStates[D3DRS_SRCBLEND] == D3DBLEND_ONE &&
+        m_state.renderStates[D3DRS_DESTBLEND] == D3DBLEND_ZERO);
+    }
+
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(m_state.vertexDecl == nullptr))
@@ -2522,6 +3080,39 @@ namespace dxvk {
           D3DFORMAT        IndexDataFormat,
     const void*            pVertexStreamZeroData,
           UINT             VertexStreamZeroStride) {
+    if (g_batchProbe.enabled) {
+      uint64_t tex = reinterpret_cast<uint64_t>(m_state.textures[0]);
+      uint64_t sig = tex
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexShader.ptr())  * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.pixelShader.ptr())   * 0xC2B2AE3D27D4EB4Full)
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexDecl.ptr())    * 0x165667B19E3779F9ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.textures[1])         * 0x27D4EB2F165667C5ull);
+      uint64_t full = sig;
+      {
+        // fold in the world transform -- if this collapses as well, draws sharing state also
+        // share their transform and can be concatenated without any instancing work.
+        const auto& wm = m_state.transforms[GetTransformIndex(D3DTS_WORLD)];
+        const float* f = reinterpret_cast<const float*>(&wm);
+        for (int i = 0; i < 16; i++) {
+          uint32_t bits; std::memcpy(&bits, &f[i], sizeof(bits));
+          full = full * 0x100000001B3ull ^ bits;
+        }
+        // and the fixed-function render states the shader reads
+        for (auto rs : { D3DRS_ALPHABLENDENABLE, D3DRS_SRCBLEND, D3DRS_DESTBLEND,
+                         D3DRS_ALPHATESTENABLE, D3DRS_ALPHAREF, D3DRS_CULLMODE,
+                         D3DRS_ZWRITEENABLE, D3DRS_ZFUNC, D3DRS_FOGENABLE })
+          full = full * 0x100000001B3ull ^ uint64_t(m_state.renderStates[rs]);
+      }
+      // mesh identity: same state AND same geometry -> this is what instancing can collapse.
+      uint64_t mesh = sig
+        ^ (reinterpret_cast<uint64_t>(m_state.vertexBuffers[0].vertexBuffer.ptr()) * 0x9E3779B97F4A7C15ull)
+        ^ (reinterpret_cast<uint64_t>(m_state.indices.ptr())                       * 0xC2B2AE3D27D4EB4Full);
+      g_batchProbe.note(sig, tex, full, mesh,
+        m_state.renderStates[D3DRS_ALPHABLENDENABLE] != 0,
+        m_state.renderStates[D3DRS_SRCBLEND] == D3DBLEND_ONE &&
+        m_state.renderStates[D3DRS_DESTBLEND] == D3DBLEND_ZERO);
+    }
+
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(m_state.vertexDecl == nullptr))
@@ -2718,6 +3309,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetVertexDeclaration(IDirect3DVertexDeclaration9* pDecl) {
+    D3D9ApiTimer apiTimer(ApiSetVertexDeclaration);
     D3D9DeviceLock lock = LockDevice();
 
     D3D9VertexDecl* decl = static_cast<D3D9VertexDecl*>(pDecl);
@@ -2762,6 +3354,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetFVF(DWORD FVF) {
+    D3D9ApiTimer apiTimer(ApiSetFVF);
     D3D9DeviceLock lock = LockDevice();
 
     if (FVF == 0)
@@ -2823,6 +3416,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetVertexShader(IDirect3DVertexShader9* pShader) {
+    D3D9ApiTimer apiTimer(ApiSetVertexShader);
     D3D9DeviceLock lock = LockDevice();
 
     D3D9VertexShader* shader = static_cast<D3D9VertexShader*>(pShader);
@@ -2888,6 +3482,7 @@ namespace dxvk {
           UINT   StartRegister,
     const float* pConstantData,
           UINT   Vector4fCount) {
+    D3D9ApiTimer apiTimer(ApiSetVertexShaderConstantF);
     D3D9DeviceLock lock = LockDevice();
 
     return SetShaderConstants<
@@ -2979,6 +3574,7 @@ namespace dxvk {
           IDirect3DVertexBuffer9* pStreamData,
           UINT                    OffsetInBytes,
           UINT                    Stride) {
+    D3D9ApiTimer apiTimer(ApiSetStreamSource);
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(StreamNumber >= caps::MaxStreams))
@@ -3098,6 +3694,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetIndices(IDirect3DIndexBuffer9* pIndexData) {
+    D3D9ApiTimer apiTimer(ApiSetIndices);
     D3D9DeviceLock lock = LockDevice();
 
     D3D9IndexBuffer* buffer = static_cast<D3D9IndexBuffer*>(pIndexData);
@@ -3155,6 +3752,7 @@ namespace dxvk {
 
 
   HRESULT STDMETHODCALLTYPE D3D9DeviceEx::SetPixelShader(IDirect3DPixelShader9* pShader) {
+    D3D9ApiTimer apiTimer(ApiSetPixelShader);
     D3D9DeviceLock lock = LockDevice();
 
     D3D9PixelShader* shader = static_cast<D3D9PixelShader*>(pShader);
@@ -3224,6 +3822,7 @@ namespace dxvk {
     UINT   StartRegister,
     const float* pConstantData,
     UINT   Vector4fCount) {
+    D3D9ApiTimer apiTimer(ApiSetPixelShaderConstantF);
     D3D9DeviceLock lock = LockDevice();
 
     return SetShaderConstants <
@@ -3892,8 +4491,13 @@ namespace dxvk {
     DxvkDeviceFeatures enabled = {};
 
     // Geometry shaders are used for some meta ops
-    enabled.core.features.geometryShader = VK_TRUE;
-    enabled.core.features.robustBufferAccess = VK_TRUE;
+    // MoltenVK 1.3+ correctly reports that Metal supports neither geometry shaders nor
+    // robust buffer access. Demanding them unconditionally makes device creation fail on
+    // every honest Metal driver, which is why DXVK on macOS was stuck on MoltenVK 1.2.10.
+    // Both are optional here: the geometry-shader meta ops are already guarded at use, and
+    // robustness only affects out-of-bounds behaviour, not correctness of in-bounds draws.
+    enabled.core.features.geometryShader = supported.core.features.geometryShader;
+    enabled.core.features.robustBufferAccess = supported.core.features.robustBufferAccess;
     enabled.extRobustness2.robustBufferAccess2 = supported.extRobustness2.robustBufferAccess2;
 
     enabled.extMemoryPriority.memoryPriority = supported.extMemoryPriority.memoryPriority;
@@ -3921,7 +4525,10 @@ namespace dxvk {
     enabled.core.features.sampleRateShading = VK_TRUE;
     enabled.core.features.samplerAnisotropy = supported.core.features.samplerAnisotropy;
     enabled.core.features.shaderClipDistance = VK_TRUE;
-    enabled.core.features.shaderCullDistance = VK_TRUE;
+    // Metal exposes clip distance but not cull distance. D3D9 only ever needs clip distance
+    // (user clip planes), so requesting cull distance unconditionally locks the whole D3D9
+    // frontend out of every Metal driver for a feature it does not use.
+    enabled.core.features.shaderCullDistance = supported.core.features.shaderCullDistance;
 
     // Ensure we support real BC formats and unofficial vendor ones.
     enabled.core.features.textureCompressionBC = VK_TRUE;
@@ -4060,7 +4667,10 @@ namespace dxvk {
   bool D3D9DeviceEx::WaitForResource(
   const Rc<DxvkResource>&                 Resource,
         uint64_t                          SequenceNumber,
-        DWORD                             MapFlags) {
+        DWORD                             MapFlags,
+        bool                              IsImage) {
+    auto stallStart = std::chrono::steady_clock::now();
+
     // Wait for the any pending D3D9 command to be executed
     // on the CS thread so that we can determine whether the
     // resource is currently in use or not.
@@ -4079,6 +4689,7 @@ namespace dxvk {
         // still try to spin on `Map` until the resource is
         // idle, so we should flush pending commands
         FlushImplicit(FALSE);
+        g_stallProbe.add(kStMapWait, stallStart);
         return false;
       }
       else {
@@ -4087,10 +4698,13 @@ namespace dxvk {
         Flush();
         SynchronizeCsThread(SequenceNumber);
 
-        m_dxvkDevice->waitForResource(Resource, access);
+        auto blockStart = std::chrono::steady_clock::now();
+        m_dxvkDevice->waitForResource(Resource, access, IsImage);
+        g_stallProbe.add(kStMapBlocked, blockStart);
       }
     }
 
+    g_stallProbe.add(kStMapWait, stallStart);
     return true;
   }
 
@@ -4121,6 +4735,74 @@ namespace dxvk {
   }
 
 
+
+  // Which texture locks stall on the GPU (D3D9_LOCKIMAGE_PROBE=<path>, off unless set).
+  //
+  // In-world, 100% of this project's GPU synchronisations come from LockImage and they cost
+  // ~22 ms of a ~32 ms frame. Whether that is fixable depends entirely on *what* is being
+  // locked -- pool, usage and size decide which of DXVK's escape hatches (DISCARD, staging
+  // upload, skipWait) is even available. Aggregated by texture kind so it costs a hash lookup
+  // per lock rather than a line of I/O.
+  namespace {
+    struct LockImageProbe {
+      struct Key {
+        uint32_t pool, usage, format, w, h, discard, readback, waited;
+        bool operator==(const Key& o) const {
+          return pool == o.pool && usage == o.usage && format == o.format && w == o.w
+              && h == o.h && discard == o.discard && readback == o.readback
+              && waited == o.waited;
+        }
+      };
+      struct Hash {
+        size_t operator()(const Key& k) const {
+          size_t h = 1469598103934665603ull;
+          for (uint32_t v : { k.pool, k.usage, k.format, k.w, k.h, k.discard, k.readback,
+                              k.waited })
+            h = (h ^ v) * 1099511628211ull;
+          return h;
+        }
+      };
+
+      FILE* file = nullptr;
+      std::mutex mutex;
+      std::unordered_map<Key, uint64_t, Hash> counts;
+      dxvk::high_resolution_clock::time_point tick;
+
+      LockImageProbe() {
+        const char* path = std::getenv("D3D9_LOCKIMAGE_PROBE");
+        if (path == nullptr || *path == '\0')
+          return;
+        file = std::fopen(path, "a");
+        if (file != nullptr) {
+          std::fprintf(file, "pool,usage,format,w,h,discard,readback,waited,locks_per_s\n");
+          std::fflush(file);
+        }
+        tick = dxvk::high_resolution_clock::now();
+      }
+
+      void record(const Key& k) {
+        if (file == nullptr)
+          return;
+        std::lock_guard<std::mutex> lock(mutex);
+        counts[k] += 1;
+        auto now = dxvk::high_resolution_clock::now();
+        double dt = std::chrono::duration<double>(now - tick).count();
+        if (dt < 2.0)
+          return;
+        for (const auto& e : counts) {
+          std::fprintf(file, "%u,0x%x,%u,%u,%u,%u,%u,%u,%.1f\n",
+            e.first.pool, e.first.usage, e.first.format, e.first.w, e.first.h,
+            e.first.discard, e.first.readback, e.first.waited, double(e.second) / dt);
+        }
+        std::fflush(file);
+        counts.clear();
+        tick = now;
+      }
+    };
+
+    static LockImageProbe g_lockImageProbe;
+  }
+
   HRESULT D3D9DeviceEx::LockImage(
             D3D9CommonTexture*      pResource,
             UINT                    Face,
@@ -4128,6 +4810,7 @@ namespace dxvk {
             D3DLOCKED_BOX*          pLockedBox,
       const D3DBOX*                 pBox,
             DWORD                   Flags) {
+    D3D9ApiTimer apiTimer(ApiLockImage);
     D3D9DeviceLock lock = LockDevice();
 
     UINT Subresource = pResource->CalcSubresource(Face, MipLevel);
@@ -4208,6 +4891,9 @@ namespace dxvk {
     DxvkBufferSliceHandle physSlice;
 
     if (Flags & D3DLOCK_DISCARD) {
+      g_lockImageProbe.record({ uint32_t(desc.Pool), uint32_t(desc.Usage),
+                                uint32_t(desc.Format), desc.Width, desc.Height, 1u,
+                                uint32_t(needsReadback), 0u });
       // We do not have to preserve the contents of the
       // buffer if the entire image gets discarded.
       physSlice = pResource->DiscardMapSlice(Subresource);
@@ -4233,7 +4919,8 @@ namespace dxvk {
         std::memset(physSlice.mapPtr, 0, physSlice.length);
       }
       else if (!skipWait) {
-        if (unlikely(needsReadback) && pResource->GetImage() != nullptr) {
+        if (unlikely(needsReadback) && pResource->GetImage() != nullptr
+         && !pResource->HasEarlyReadback()) {
           Rc<DxvkImage> resourceImage = pResource->GetImage();
 
           Rc<DxvkImage> mappedImage = resourceImage->info().sampleCount != 1
@@ -4303,11 +4990,123 @@ namespace dxvk {
             }
           });
           TrackTextureMappingBufferSequenceNumber(pResource, Subresource);
-        } else if (!(Flags & D3DLOCK_DONOTWAIT) && !WaitForResource(mappedBuffer, pResource->GetMappingBufferSequenceNumber(Subresource), D3DLOCK_DONOTWAIT)) {
+        } else if (!(Flags & D3DLOCK_DONOTWAIT) && !WaitForResource(mappedBuffer, pResource->GetMappingBufferSequenceNumber(Subresource), D3DLOCK_DONOTWAIT, true)) {
           pResource->EnableStagingBufferUploads(Subresource);
         }
 
-        if (!WaitForResource(mappedBuffer, pResource->GetMappingBufferSequenceNumber(Subresource), Flags))
+        g_lockImageProbe.record({ uint32_t(desc.Pool), uint32_t(desc.Usage),
+                                  uint32_t(desc.Format), desc.Width, desc.Height,
+                                  uint32_t((Flags & D3DLOCK_DISCARD) != 0),
+                                  uint32_t(needsReadback), 1u });
+
+        // D3D9_RT_READBACK_NOWAIT=<max edge in px>: do not block on the read-back of a small
+        // render target.
+        //
+        // FFXI reads a 16x16 D3DUSAGE_RENDERTARGET texture back about 5.4 times per frame --
+        // the shape of an occlusion / lens-flare visibility test. Each read-back drains the
+        // pipeline: measured in town it is ~22 ms of a ~32 ms frame, and it is the reason the
+        // CPU and GPU run in lockstep with the GPU only ~70% busy. The copy is still issued, so
+        // the buffer keeps being refreshed; skipping only the wait means the game reads the
+        // previous frame's result instead of this frame's. For a visibility factor driving a
+        // flare's brightness, one frame of lag is not observable -- but this is a semantic
+        // change, so it is opt-in and bounded to small surfaces.
+        static const uint32_t rtNoWaitMaxEdge = [] {
+          const char* v = std::getenv("D3D9_RT_READBACK_NOWAIT");
+          return (v != nullptr && *v != '\0') ? uint32_t(std::atoi(v)) : 0u;
+        }();
+
+        const bool smallRtReadback = rtNoWaitMaxEdge != 0
+          && needsReadback
+          && desc.Pool == D3DPOOL_DEFAULT
+          && (desc.Usage & D3DUSAGE_RENDERTARGET)
+          && desc.Width  <= rtNoWaitMaxEdge
+          && desc.Height <= rtNoWaitMaxEdge;
+
+        // D3D9_RT_READBACK_FENCE=<max edge in px>: wait for the read-back's own fence
+        // instead of the retirement thread's serialized progress. EXACT, unlike NOWAIT:
+        // the copy's fence signaling means the staging buffer holds this frame's bytes, so
+        // the game reads the same values the slow path would give it. What it skips is only
+        // the retirement thread's one-at-a-time fence processing (~0.85 ms per submission,
+        // ~15 submissions/frame measured 2026-08-20 = most of the 26 ms stall).
+        static const uint32_t rtFenceMaxEdge = [] {
+          const char* v = std::getenv("D3D9_RT_READBACK_FENCE");
+          return (v != nullptr && *v != '\0') ? uint32_t(std::atoi(v)) : 0u;
+        }();
+
+        const bool smallRtFence = rtFenceMaxEdge != 0
+          && needsReadback
+          && desc.Pool == D3DPOOL_DEFAULT
+          && (desc.Usage & D3DUSAGE_RENDERTARGET)
+          && desc.Width  <= rtFenceMaxEdge
+          && desc.Height <= rtFenceMaxEdge;
+
+        // D3D9_RT_IDENT_PROBE=<path>: one line per small-RT lock — microsecond timestamp and
+        // the texture's identity. Answers whether the ~4 locks per frame hit one surface
+        // repeatedly (dedup-able: serve one result per frame) or four distinct surfaces
+        // (dedup needs per-texture caching or is pointless).
+        static FILE* rtIdentProbe = [] () -> FILE* {
+          const char* v = std::getenv("D3D9_RT_IDENT_PROBE");
+          return (v != nullptr && *v != '\0') ? std::fopen(v, "a") : nullptr;
+        }();
+        if (rtIdentProbe != nullptr && (smallRtFence || smallRtReadback)) {
+          auto us = std::chrono::duration_cast<std::chrono::microseconds>(
+            std::chrono::steady_clock::now().time_since_epoch()).count();
+          std::fprintf(rtIdentProbe, "%lld,L,%p\n", (long long)us, (void*)pResource);
+          std::fflush(rtIdentProbe);
+        }
+
+        bool fenceSatisfied = false;
+        if (smallRtFence && !smallRtReadback) {
+          auto stallStart = std::chrono::steady_clock::now();
+          // With an early read-back pending (D3D9_RT_EARLY_FENCE), the copy was flushed
+          // when the render target was unbound — no lock-time flush needed, and the wait
+          // lands on a submission that is already executing.
+          Rc<DxvkCommandList> earlyPeek = pResource->HasEarlyReadback()
+            ? pResource->TakeEarlyReadback() : nullptr;
+          if (earlyPeek == nullptr) {
+            // Get the copy onto the GPU: emit pending CS work and submit it.
+            Flush();
+            // Full CS sync, not just to the copy's sequence: the flush's own submission is
+            // processed by the CS thread *after* the copy, and lastSubmittedList() must not
+            // be captured before that submission is enqueued — an earlier capture waits an
+            // older fence and could hand the game a stale read-back (the NOWAIT bug).
+            SynchronizeCsThread(DxvkCsThread::SynchronizeAll);
+          }
+          // The submission created by that flush contains our copy (it was recorded before
+          // this lock). Wait on its fence directly, bounded: the list object can be recycled
+          // by the retirement thread (fence reset), so cap each wait and re-check the
+          // resource; fall back to the exact slow path if this never converges.
+          Rc<DxvkCommandList> list = earlyPeek != nullptr
+            ? std::move(earlyPeek)
+            : m_dxvkDevice->lastSubmittedList();
+          // D3D9_RT_FENCE_PROBE=<path>: per lock, the pending-submission depth at wait start
+          // and the microseconds the fence wait took. Decides the side-queue architecture:
+          // depth ~1 with a long wait = the pre-lock submission's own execution dominates
+          // (a second queue cannot help; the copy waits on its own pass); depth >> 1 = the
+          // wait is queued-behind work and a second VkQueue/MTLCommandQueue wins.
+          static FILE* fenceProbe = [] () -> FILE* {
+            const char* v = std::getenv("D3D9_RT_FENCE_PROBE");
+            return (v != nullptr && *v != '\0') ? std::fopen(v, "a") : nullptr;
+          }();
+          uint32_t fpDepth = fenceProbe ? m_dxvkDevice->pendingSubmissions() : 0u;
+          auto fpT0 = std::chrono::steady_clock::now();
+          if (list != nullptr) {
+            for (uint32_t i = 0; i < 100 && !fenceSatisfied; i++) {
+              if (!mappedBuffer->isInUse(DxvkAccess::Write)) { fenceSatisfied = true; break; }
+              if (list->synchronizeBounded(500'000ull) == VK_SUCCESS)
+                fenceSatisfied = true;
+            }
+          }
+          if (fenceProbe != nullptr) {
+            auto us = std::chrono::duration_cast<std::chrono::microseconds>(
+              std::chrono::steady_clock::now() - fpT0).count();
+            std::fprintf(fenceProbe, "%u,%lld\n", fpDepth, (long long)us);
+          }
+          g_stallProbe.add(kStMapWait, stallStart);
+        }
+
+        if (!smallRtReadback && !fenceSatisfied &&
+            !WaitForResource(mappedBuffer, pResource->GetMappingBufferSequenceNumber(Subresource), Flags, true))
           return D3DERR_WASSTILLDRAWING;
       }
     }
@@ -4588,6 +5387,7 @@ namespace dxvk {
           UINT                    SizeToLock,
           void**                  ppbData,
           DWORD                   Flags) {
+    D3D9ApiTimer apiTimer(ApiLockBuffer);
     D3D9DeviceLock lock = LockDevice();
 
     if (unlikely(ppbData == nullptr))
@@ -4773,25 +5573,50 @@ namespace dxvk {
   }
 
 
+
+
   void D3D9DeviceEx::FlushImplicit(BOOL StrongHint) {
+    g_flushProbe.call(g_currentSite);
     // Flush only if the GPU is about to go idle, in
     // order to keep the number of submissions low.
     uint32_t pending = m_dxvkDevice->pendingSubmissions();
 
+    // Submission pacing, made tunable (DXVK_FLUSH_INTERVAL_US, DXVK_NO_STRONG_FLUSH=1).
+    //
+    // The 750 us floor was chosen for Vulkan drivers where a submit is cheap. On Metal through
+    // MoltenVK each submit is a command-buffer commit, and FFXI in town was measured at ~30
+    // submits *per frame* -- it calls this on every texture upload and on every change of
+    // render target 0, and a city streams textures continuously. Raising the interval trades
+    // latency for far fewer commits.
+    static const uint32_t minFlushUs = [] {
+      const char* v = std::getenv("DXVK_FLUSH_INTERVAL_US");
+      return (v && *v) ? uint32_t(std::atoi(v)) : MinFlushIntervalUs;
+    }();
+    static const bool noStrongFlush = [] {
+      const char* v = std::getenv("DXVK_NO_STRONG_FLUSH");
+      return v && *v && *v != '0';
+    }();
+
+    if (noStrongFlush)
+      StrongHint = FALSE;
+
     if (StrongHint || pending <= MaxPendingSubmits) {
       auto now = dxvk::high_resolution_clock::now();
 
-      uint32_t delay = MinFlushIntervalUs
+      uint32_t delay = minFlushUs
                      + IncFlushIntervalUs * pending;
 
       // Prevent flushing too often in short intervals.
-      if (now - m_lastFlush >= std::chrono::microseconds(delay))
+      if (now - m_lastFlush >= std::chrono::microseconds(delay)) {
+        g_flushProbe.flushed(g_currentSite);
         Flush();
+      }
     }
   }
 
 
   void D3D9DeviceEx::SynchronizeCsThread(uint64_t SequenceNumber) {
+    auto stallStart = std::chrono::steady_clock::now();
     D3D9DeviceLock lock = LockDevice();
 
     // Dispatch current chunk so that all commands
@@ -4799,6 +5624,7 @@ namespace dxvk {
     FlushCsChunk();
 
     m_csThread.synchronize(SequenceNumber);
+    g_stallProbe.add(kStSyncCs, stallStart);
   }
 
 
@@ -5553,6 +6379,58 @@ namespace dxvk {
   }
 
 
+  // Framebuffer-change probe (DXVK_FB_PROBE=<path>). Every framebuffer change spills the
+  // render pass, and on Metal that means a new command encoder. This records *what* about the
+  // attachment set changed, so the cause can be fixed instead of guessed at.
+  namespace {
+    struct D3D9FbProbe {
+      FILE* file = nullptr;
+      std::map<std::string, uint64_t> counts;
+      std::chrono::steady_clock::time_point tick;
+      VkImageView lastDepth = VK_NULL_HANDLE;
+      VkImageLayout lastDepthLayout = VK_IMAGE_LAYOUT_UNDEFINED;
+      VkImageView lastColor0 = VK_NULL_HANDLE;
+      bool first = true;
+
+      D3D9FbProbe() {
+        const char* p = std::getenv("DXVK_FB_PROBE");
+        if (p && *p)
+          file = std::fopen(p, "a");
+        tick = std::chrono::steady_clock::now();
+      }
+
+      void note(VkImageView depth, VkImageLayout depthLayout, VkImageView color0) {
+        if (!file)
+          return;
+        if (first) {
+          first = false;
+        } else if (depth != lastDepth) {
+          counts[(depth == VK_NULL_HANDLE) ? "depth-detached" :
+                 (lastDepth == VK_NULL_HANDLE) ? "depth-attached" : "depth-view"] += 1;
+        } else if (depthLayout != lastDepthLayout) {
+          counts["depth-layout"] += 1;
+        } else if (color0 != lastColor0) {
+          counts[(color0 == VK_NULL_HANDLE) ? "color0-detached" :
+                 (lastColor0 == VK_NULL_HANDLE) ? "color0-attached" : "color0-view"] += 1;
+        } else {
+          counts["no-visible-change"] += 1;
+        }
+        lastDepth = depth;
+        lastDepthLayout = depthLayout;
+        lastColor0 = color0;
+
+        auto now = std::chrono::steady_clock::now();
+        if (std::chrono::duration<double>(now - tick).count() < 5.0)
+          return;
+        tick = now;
+        std::fprintf(file, "--- framebuffer changes\n");
+        for (const auto& e : counts)
+          std::fprintf(file, "%8llu  %s\n", (unsigned long long) e.second, e.first.c_str());
+        std::fflush(file);
+      }
+    };
+  }
+
   void D3D9DeviceEx::BindFramebuffer() {
     m_flags.clr(D3D9DeviceFlag::DirtyFramebuffer);
 
@@ -5581,8 +6459,25 @@ namespace dxvk {
         m_state.renderTargets[i]->GetRenderTargetLayout() };
     }
 
+    // DXVK_KEEP_DEPTH=1 keeps the depth-stencil attached even while depth and stencil are
+    // disabled. Detaching it changes the attachment set, which forces a new framebuffer and
+    // spills the render pass, and FFXI toggles ZENABLE/ZWRITEENABLE constantly (world geometry
+    // vs UI) -- roughly 24,000 render-pass breaks per session. It is the architecturally
+    // correct fix and it does remove them.
+    //
+    // It is off by default anyway, because it was measured and it does not pay: those breaks
+    // are only ~700 render passes a second, under 2% of the frame, and keeping the attachment
+    // bound adds depth load/store work that made the measured frame rate slightly *worse*
+    // (11.6 vs 12.9 fps). Kept because it is the right answer on a driver where render passes
+    // are expensive, and because the measurement is worth being able to repeat.
+    static const bool keepDepthAttached = [] {
+      const char* v = std::getenv("DXVK_KEEP_DEPTH");
+      return v != nullptr && std::strcmp(v, "0") != 0;
+    }();
+
     if (m_state.depthStencil != nullptr &&
-      (m_state.renderStates[D3DRS_ZENABLE]
+      (keepDepthAttached
+        || m_state.renderStates[D3DRS_ZENABLE]
         || m_state.renderStates[D3DRS_ZWRITEENABLE]
         || m_state.renderStates[D3DRS_STENCILENABLE])) {
       const DxvkImageCreateInfo& dsImageInfo = m_state.depthStencil->GetCommonTexture()->GetImage()->info();
@@ -5595,6 +6490,12 @@ namespace dxvk {
       }
     }
 
+    static D3D9FbProbe fbProbe;
+    fbProbe.note(
+      attachments.depth.view    != nullptr ? attachments.depth.view->handle()    : VK_NULL_HANDLE,
+      attachments.depth.layout,
+      attachments.color[0].view != nullptr ? attachments.color[0].view->handle() : VK_NULL_HANDLE);
+
     // Create and bind the framebuffer object to the context
     EmitCs([
       cAttachments = std::move(attachments)
@@ -6429,6 +7330,7 @@ namespace dxvk {
 
 
   void D3D9DeviceEx::End(D3D9Query* pQuery) {
+    g_currentSite = kFsQueryEnd;
     D3D9DeviceLock lock = LockDevice();
 
     EmitCs([cQuery = Com<D3D9Query, false>(pQuery)](DxvkContext* ctx) {
diff --git a/src/d3d9/d3d9_device.h b/src/d3d9/d3d9_device.h
index cec4829..8e1e04a 100644
--- a/src/d3d9/d3d9_device.h
+++ b/src/d3d9/d3d9_device.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 #include "../dxvk/dxvk_cs.h"
 
@@ -665,7 +668,8 @@ namespace dxvk {
     bool WaitForResource(
       const Rc<DxvkResource>&                 Resource,
             uint64_t                          SequenceNumber,
-            DWORD                             MapFlags);
+            DWORD                             MapFlags,
+            bool                              IsImage = false);
 
     /**
      * \brief Locks a subresource of an image
diff --git a/src/d3d9/d3d9_device_child.h b/src/d3d9/d3d9_device_child.h
index 433a269..1b62f06 100644
--- a/src/d3d9/d3d9_device_child.h
+++ b/src/d3d9/d3d9_device_child.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 namespace dxvk {
diff --git a/src/d3d9/d3d9_fixed_function.cpp b/src/d3d9/d3d9_fixed_function.cpp
index 37f6e1a..b03453d 100644
--- a/src/d3d9/d3d9_fixed_function.cpp
+++ b/src/d3d9/d3d9_fixed_function.cpp
@@ -1,4 +1,6 @@
 #include "d3d9_fixed_function.h"
+#include <cstdlib>
+#include <string>
 
 #include "d3d9_device.h"
 #include "d3d9_util.h"
@@ -182,6 +184,36 @@ namespace dxvk {
       std::array<uint32_t, 3> fogFacIndices = { fogFactor, fogFactor, fogFactor };
       uint32_t fogFact3 = spvModule.opCompositeConstruct(vec3Type, fogFacIndices.size(), fogFacIndices.data());
 
+      // Diagnostic: DXVK_DEBUG_SHOW=end|scale|density|depth|color paints that input as the
+      // colour, with fog disabled, so the value can be read straight off the screen.
+      if (const char* what = std::getenv("DXVK_DEBUG_SHOW")) {
+        uint32_t v = 0;
+        std::string w(what);
+        if      (w == "end")     v = spvModule.opFMul(floatType, fogEnd, spvModule.constf32(0.001f));
+        else if (w == "scale")   v = spvModule.opFMul(floatType, fogScale, spvModule.constf32(100.0f));
+        else if (w == "density") v = fogDensity;
+        else if (w == "depth")   v = spvModule.opFMul(floatType, depth, spvModule.constf32(0.001f));
+        else                     v = spvModule.constf32(0.5f);
+        if (w == "color") {
+          color3 = fogColor;
+        } else {
+          std::array<uint32_t,3> vv = { v, v, v };
+          color3 = spvModule.opCompositeConstruct(vec3Type, vv.size(), vv.data());
+        }
+        std::array<uint32_t,3> one = { spvModule.constf32(1.0f), spvModule.constf32(1.0f), spvModule.constf32(1.0f) };
+        fogFact3 = spvModule.opCompositeConstruct(vec3Type, one.size(), one.data());
+      }
+
+      // Diagnostic: paint the computed fog factor as greyscale. White = factor 1 (no fog,
+      // picture should be intact), black = factor 0 (fully fogged -> the fog colour wins).
+      // This separates "fog inputs are wrong" from "the constants never arrived".
+      if (std::getenv("DXVK_DEBUG_FOGFACTOR") != nullptr)
+        color3 = fogFact3;
+      // Diagnostic: force no fog while keeping the whole fog code path compiled and running.
+      if (std::getenv("DXVK_DEBUG_FOGFACTOR_ONE") != nullptr)
+        fogFact3 = spvModule.opCompositeConstruct(vec3Type, 3, std::array<uint32_t,3>{
+          spvModule.constf32(1.0f), spvModule.constf32(1.0f), spvModule.constf32(1.0f) }.data());
+
       uint32_t lerpedFrog = spvModule.opFMix(vec3Type, fogColor, color3, fogFact3);
 
       fogRetValue = spvModule.opVectorShuffle(vec4Type, lerpedFrog, color, indices.size(), indices.data());
@@ -718,7 +750,14 @@ namespace dxvk {
     info.inputMask = m_inputMask;
     info.outputMask = m_outputMask;
     info.pushConstOffset = m_pushConstOffset;
-    info.pushConstSize = m_pushConstOffset;
+    // Was `= m_pushConstOffset`. For fixed-function PIXEL shaders that offset is 0, so the
+    // declared push-constant size came out 0 and DxvkShader::defineResourceSlots skipped
+    // definePushConstRange entirely -- the pipeline layout never listed the fragment stage.
+    // Desktop drivers bind push constants regardless and hid this; MoltenVK honours the
+    // declaration, so render_state_t (fog colour/scale/end/density, alpha ref) arrived as
+    // zeroes in every FFP fragment shader. Fog factor then clamped to 0 and every lit
+    // surface became the fog colour -- the "black world" on Metal.
+    info.pushConstSize = m_pushConstSize;
 
     return new DxvkShader(info, m_module.compile());
   }
@@ -1447,6 +1486,32 @@ namespace dxvk {
     if (m_vsKey.Data.Contents.VertexBlendMode == D3D9FF_VertexBlendMode_Normal)
       emitVertexBlendDecl();
 
+    // Fixed-function instancing (DXVK_FF_INSTANCING=1).
+    //
+    // FFXI redraws the same meshes over and over -- measured at 1891 draws collapsing into
+    // 191 distinct mesh+state pairs, 90% of draws repeating one already issued that frame.
+    // The only thing stopping those from being one instanced draw is that D3D9 supplies the
+    // world transform as *device state*, so each repeat needs its own WorldView matrix.
+    //
+    // DXVK already carries a storage buffer of WorldView matrices for vertex blending
+    // (D3D9FF_VertexBlendData). Here we reuse it as an instance-transform array and index it
+    // by gl_InstanceIndex, so a batch of N repeats of one mesh becomes a single draw with
+    // instanceCount = N. Off unless the env var is set; the CPU-side batching that fills the
+    // array and issues the instanced draw is a separate piece of work.
+    const bool ffInstancing = std::getenv("DXVK_FF_INSTANCING") != nullptr;
+    uint32_t instanceIndexVar = 0;
+    if (ffInstancing) {
+      if (m_vsKey.Data.Contents.VertexBlendMode != D3D9FF_VertexBlendMode_Normal)
+        emitVertexBlendDecl();
+
+      instanceIndexVar = m_module.newVar(
+        m_module.defPointerType(m_uint32Type, spv::StorageClassInput),
+        spv::StorageClassInput);
+      m_module.decorateBuiltIn(instanceIndexVar, spv::BuiltInInstanceIndex);
+      m_module.setDebugName(instanceIndexVar, "vs_instance_index");
+      m_entryPointInterfaces.push_back(instanceIndexVar);
+    }
+
     // Load constants
     auto LoadConstant = [&](uint32_t type, uint32_t idx) {
       uint32_t offset  = m_module.constu32(idx);
@@ -1457,6 +1522,16 @@ namespace dxvk {
     };
 
     m_vs.constants.worldview = LoadConstant(m_mat4Type, uint32_t(D3D9FFVSMembers::WorldViewMatrix));
+
+    if (ffInstancing) {
+      // WorldViewArray[gl_InstanceIndex] replaces the single device-state WorldView.
+      uint32_t instanceId = m_module.opLoad(m_uint32Type, instanceIndexVar);
+      std::array<uint32_t, 2> idx = { m_module.constu32(0), instanceId };
+      m_vs.constants.worldview = m_module.opLoad(m_mat4Type,
+        m_module.opAccessChain(
+          m_module.defPointerType(m_mat4Type, spv::StorageClassUniform),
+          m_vs.vertexBlendData, idx.size(), idx.data()));
+    }
     m_vs.constants.normal    = LoadConstant(m_mat4Type, uint32_t(D3D9FFVSMembers::NormalMatrix));
     m_vs.constants.inverseView = LoadConstant(m_mat4Type, uint32_t(D3D9FFVSMembers::InverseViewMatrix));
     m_vs.constants.proj      = LoadConstant(m_mat4Type, uint32_t(D3D9FFVSMembers::ProjMatrix));
diff --git a/src/d3d9/d3d9_fixed_function.h b/src/d3d9/d3d9_fixed_function.h
index 8e312aa..a183ca2 100644
--- a/src/d3d9/d3d9_fixed_function.h
+++ b/src/d3d9/d3d9_fixed_function.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 #include "d3d9_caps.h"
diff --git a/src/d3d9/d3d9_format.h b/src/d3d9/d3d9_format.h
index 0c9ab16..499d233 100644
--- a/src/d3d9/d3d9_format.h
+++ b/src/d3d9/d3d9_format.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <d3d9.h>
 #include "d3d9_options.h"
 
diff --git a/src/d3d9/d3d9_format_helpers.h b/src/d3d9/d3d9_format_helpers.h
index 16348ed..589dcfd 100644
--- a/src/d3d9/d3d9_format_helpers.h
+++ b/src/d3d9/d3d9_format_helpers.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 #include "d3d9_format.h"
 #include "../dxvk/dxvk_device.h"
diff --git a/src/d3d9/d3d9_include.h b/src/d3d9/d3d9_include.h
index 8762265..9d89f12 100644
--- a/src/d3d9/d3d9_include.h
+++ b/src/d3d9/d3d9_include.h
@@ -79,10 +79,7 @@
 
 // MinGW headers are broken. Who'dve guessed?
 #ifndef _MSC_VER
-typedef struct _D3DDEVINFO_RESOURCEMANAGER
-{
-  char dummy;
-} D3DDEVINFO_RESOURCEMANAGER, * LPD3DDEVINFO_RESOURCEMANAGER;
+// mingw-w64 14 defines this itself now; DXVK's old workaround is the conflict.
 
 #ifndef __WINE__
 extern "C" WINUSERAPI WINBOOL WINAPI SetProcessDPIAware(VOID);
diff --git a/src/d3d9/d3d9_initializer.h b/src/d3d9/d3d9_initializer.h
index 59802f9..8e290b7 100644
--- a/src/d3d9/d3d9_initializer.h
+++ b/src/d3d9/d3d9_initializer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_common_buffer.h"
 #include "d3d9_common_texture.h"
 
diff --git a/src/d3d9/d3d9_monitor.h b/src/d3d9/d3d9_monitor.h
index 0a9d1a8..cab252e 100644
--- a/src/d3d9/d3d9_monitor.h
+++ b/src/d3d9/d3d9_monitor.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 #include "d3d9_format.h"
diff --git a/src/d3d9/d3d9_options.h b/src/d3d9/d3d9_options.h
index 17ffb41..5ca2a9a 100644
--- a/src/d3d9/d3d9_options.h
+++ b/src/d3d9/d3d9_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/config/config.h"
 #include "../dxvk/dxvk_device.h"
 
diff --git a/src/d3d9/d3d9_query.h b/src/d3d9/d3d9_query.h
index 8d742c9..095b5d6 100644
--- a/src/d3d9/d3d9_query.h
+++ b/src/d3d9/d3d9_query.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device_child.h"
 
 #include "../dxvk/dxvk_context.h"
diff --git a/src/d3d9/d3d9_sampler.h b/src/d3d9/d3d9_sampler.h
index 4447109..7040738 100644
--- a/src/d3d9/d3d9_sampler.h
+++ b/src/d3d9/d3d9_sampler.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_include.h"
 
 #include "d3d9_util.h"
diff --git a/src/d3d9/d3d9_shader.h b/src/d3d9/d3d9_shader.h
index d896f01..72a718b 100644
--- a/src/d3d9/d3d9_shader.h
+++ b/src/d3d9/d3d9_shader.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_resource.h"
 #include "../dxso/dxso_module.h"
 #include "d3d9_shader_permutations.h"
diff --git a/src/d3d9/d3d9_state.h b/src/d3d9/d3d9_state.h
index d87b8f5..cda1b3e 100644
--- a/src/d3d9/d3d9_state.h
+++ b/src/d3d9/d3d9_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_caps.h"
 #include "d3d9_constant_set.h"
 #include "../dxso/dxso_common.h"
diff --git a/src/d3d9/d3d9_stateblock.h b/src/d3d9/d3d9_stateblock.h
index 43a712a..e0bcc25 100644
--- a/src/d3d9/d3d9_stateblock.h
+++ b/src/d3d9/d3d9_stateblock.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device_child.h"
 #include "d3d9_device.h"
 #include "d3d9_state.h"
diff --git a/src/d3d9/d3d9_swapchain.cpp b/src/d3d9/d3d9_swapchain.cpp
index 306a79b..8888818 100644
--- a/src/d3d9/d3d9_swapchain.cpp
+++ b/src/d3d9/d3d9_swapchain.cpp
@@ -1,4 +1,9 @@
 #include "d3d9_swapchain.h"
+
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
+#include <tlhelp32.h>
 #include "d3d9_surface.h"
 #include "d3d9_monitor.h"
 
@@ -244,12 +249,732 @@ namespace dxvk {
   }
 
 
+  // Present-timing probe (DXVK_PRESENT_PROBE=<path>). Splits the frame into time spent inside
+  // Present() -- our translation stack and the GPU handoff -- and time spent outside it, which
+  // is the game's own per-frame work plus whatever it does to pace itself. Tells apart "the
+  // renderer is slow" from "the client is limiting its own frame rate".
+  namespace {
+    struct D3D9PresentProbe {
+      FILE* file = nullptr;
+      std::chrono::steady_clock::time_point tick;
+      std::chrono::steady_clock::time_point lastExit;
+      double inPresent = 0.0;
+      double outside = 0.0;
+      uint64_t frames = 0;
+      bool haveExit = false;
+
+      D3D9PresentProbe() {
+        const char* p = std::getenv("DXVK_PRESENT_PROBE");
+        if (p && *p)
+          file = std::fopen(p, "a");
+        if (file)
+          std::fprintf(file, "fps,in_present_ms,outside_ms\n");
+        tick = std::chrono::steady_clock::now();
+      }
+
+      void enter(std::chrono::steady_clock::time_point t) {
+        if (file && haveExit)
+          outside += std::chrono::duration<double, std::milli>(t - lastExit).count();
+      }
+
+      void exit(std::chrono::steady_clock::time_point tEnter) {
+        if (!file)
+          return;
+        auto now = std::chrono::steady_clock::now();
+        inPresent += std::chrono::duration<double, std::milli>(now - tEnter).count();
+        lastExit = now;
+        haveExit = true;
+        frames += 1;
+        double dt = std::chrono::duration<double>(now - tick).count();
+        if (dt < 1.0)
+          return;
+        std::fprintf(file, "%.2f,%.2f,%.2f\n", double(frames) / dt,
+          inPresent / double(frames), outside / double(frames));
+        std::fflush(file);
+        tick = now;
+        frames = 0;
+        inPresent = 0.0;
+        outside = 0.0;
+      }
+    };
+    static D3D9PresentProbe g_presentProbe;
+  }
+
+  // Wait probe (DXVK_WAIT_LOG=<path>).
+  //
+  // Per-thread CPU accounting settled that the client is *waiting*, not computing: 1.75 s of CPU
+  // across a 40 s in-world window while presenting at 12 fps. That says a blocking call is eating
+  // the frame, but not which one. This patches FFXiMain.dll's import table so every blocking
+  // entry point it calls goes through a timer first, and writes one CSV row per second with the
+  // count and total milliseconds for each. Nothing here changes behaviour -- each thunk calls
+  // straight through to the original -- and none of it runs unless the variable is set.
+  namespace {
+    enum WaitFn {
+      kSleep, kSleepEx, kWaitSingle, kWaitSingleEx, kWaitMulti, kWaitMultiEx,
+      kSelect, kRecv, kWSAWaitMulti, kWSAEnumEvents, kWaitMsg, kNtDelay, kFrameLatency,
+      kNumWaitFns
+    };
+
+    static const char* const kWaitNames[kNumWaitFns] = {
+      "Sleep", "SleepEx", "WaitForSingleObject", "WaitForSingleObjectEx",
+      "WaitForMultipleObjects", "WaitForMultipleObjectsEx", "select", "recv",
+      "WSAWaitForMultipleEvents", "WSAEnumNetworkEvents", "MsgWaitForMultipleObjects",
+      "NtDelayExecution", "SyncFrameLatency" };
+
+    struct WaitProbe {
+      FILE* file = nullptr;
+      std::atomic<uint64_t> count[kNumWaitFns] = { };
+      std::atomic<uint64_t> nanos[kNumWaitFns] = { };
+      std::chrono::steady_clock::time_point tick;
+      uint64_t frames = 0;
+      bool hooked = false;
+
+      WaitProbe() {
+        const char* p = std::getenv("DXVK_WAIT_LOG");
+        if (p && *p)
+          file = std::fopen(p, "a");
+        if (file) {
+          std::fprintf(file, "fps,wall_ms,frame_user_ms,frame_kernel_ms");
+          for (size_t i = 0; i < kNumWaitFns; i++)
+              std::fprintf(file, ",%s_n,%s_ms,%s_mainn,%s_mainms",
+              kWaitNames[i], kWaitNames[i], kWaitNames[i], kWaitNames[i]);
+          std::fprintf(file, "\n");
+          std::fflush(file);
+        }
+        tick = std::chrono::steady_clock::now();
+      }
+
+      bool on() const { return file != nullptr; }
+
+      // The thread that calls Present is the frame thread; only its waits can cost frame rate.
+      // Clamping every thread's short sleeps moved 15 s of CPU and not one frame, because the
+      // sleeping was happening off the frame path -- so the counters are split by thread now.
+      std::atomic<uint32_t> presentThread = { 0 };
+      std::atomic<uint64_t> countMain[kNumWaitFns] = { };
+      std::atomic<uint64_t> nanosMain[kNumWaitFns] = { };
+
+      void add(WaitFn fn, std::chrono::steady_clock::time_point t0) {
+        auto dt = std::chrono::steady_clock::now() - t0;
+        uint64_t ns = uint64_t(std::chrono::duration_cast<std::chrono::nanoseconds>(dt).count());
+        count[fn].fetch_add(1, std::memory_order_relaxed);
+        nanos[fn].fetch_add(ns, std::memory_order_relaxed);
+        if (GetCurrentThreadId() == presentThread.load(std::memory_order_relaxed)) {
+          countMain[fn].fetch_add(1, std::memory_order_relaxed);
+          nanosMain[fn].fetch_add(ns, std::memory_order_relaxed);
+        }
+      }
+
+      // Frame-thread CPU accounting. The frame thread turned out to make no blocking calls at
+      // all, so the question became whether its ~77 ms per frame is spent executing or parked in
+      // the kernel. GetThreadTimes answers it directly: user + kernel time for this thread only,
+      // against the wall clock over the same second.
+      uint64_t prevUser = 0, prevKernel = 0;
+      bool haveTimes = false;
+
+      void frame() {
+        if (!file)
+          return;
+        frames += 1;
+        auto now = std::chrono::steady_clock::now();
+        double dt = std::chrono::duration<double>(now - tick).count();
+        if (dt < 1.0)
+          return;
+
+        double userMs = 0.0, kernelMs = 0.0;
+        FILETIME ftC, ftE, ftK, ftU;
+        if (GetThreadTimes(GetCurrentThread(), &ftC, &ftE, &ftK, &ftU)) {
+          auto to64 = [](const FILETIME& f) {
+            return (uint64_t(f.dwHighDateTime) << 32) | f.dwLowDateTime; };
+          uint64_t u = to64(ftU), k = to64(ftK);
+          if (haveTimes) {
+            userMs   = double(u - prevUser)   / 10000.0;   // 100 ns units
+            kernelMs = double(k - prevKernel) / 10000.0;
+          }
+          prevUser = u; prevKernel = k; haveTimes = true;
+        }
+
+        std::fprintf(file, "%.2f", double(frames) / dt);
+        std::fprintf(file, ",%.1f,%.1f,%.1f", dt * 1000.0, userMs, kernelMs);
+        for (size_t i = 0; i < kNumWaitFns; i++) {
+          uint64_t n  = count[i].exchange(0, std::memory_order_relaxed);
+          uint64_t ns = nanos[i].exchange(0, std::memory_order_relaxed);
+          uint64_t nm = countMain[i].exchange(0, std::memory_order_relaxed);
+          uint64_t nsm = nanosMain[i].exchange(0, std::memory_order_relaxed);
+          std::fprintf(file, ",%llu,%.2f,%llu,%.2f", (unsigned long long) n, double(ns) / 1.0e6,
+            (unsigned long long) nm, double(nsm) / 1.0e6);
+        }
+        std::fprintf(file, "\n");
+        std::fflush(file);
+        tick = now;
+        frames = 0;
+      }
+    };
+
+    static WaitProbe g_waitProbe;
+
+    // Sampling profiler (DXVK_SAMPLE_LOG=<path>).
+    //
+    // The frame thread runs flat out -- 1040 ms of user CPU per 1058 ms of wall clock -- so the
+    // question is no longer "what is it waiting for" but "what code is it running". A helper
+    // thread suspends the frame thread on a fixed interval, reads its instruction pointer, and
+    // attributes the sample to whichever module the address falls in. That is a real profile of
+    // translated 32-bit code, which the host-side tools cannot produce: `sample` cannot unwind
+    // through this stack and reports two meaningless addresses.
+    struct ModuleBucket {
+      char     name[64] = { };
+      uintptr_t lo = 0, hi = 0;
+      uint64_t hits = 0;
+    };
+
+    struct SampleProfiler {
+      FILE* file = nullptr;
+      HANDLE thread = nullptr;
+      std::atomic<bool> stop = { false };
+      ModuleBucket mods[64] = { };
+      size_t modCount = 0;
+      uint64_t unknown = 0, total = 0;
+
+      bool on() const { return file != nullptr; }
+
+      void addModule(const char* name) {
+        HMODULE m = GetModuleHandleA(name);
+        if (m == nullptr || modCount >= 64)
+          return;
+        auto base = reinterpret_cast<uint8_t*>(m);
+        auto dos  = reinterpret_cast<IMAGE_DOS_HEADER*>(base);
+        auto nt   = reinterpret_cast<IMAGE_NT_HEADERS*>(base + dos->e_lfanew);
+        auto& b = mods[modCount++];
+        std::snprintf(b.name, sizeof(b.name), "%s", name);
+        b.lo = uintptr_t(base);
+        b.hi = b.lo + nt->OptionalHeader.SizeOfImage;
+      }
+
+      // Per-symbol counts for the hot module. Knowing the time is in ntdll is not enough: a
+      // sample parked in NtWaitForSingleObject means the thread is blocked, one in the wow64
+      // transition thunk means it is paying per-call overhead, and those lead opposite ways.
+      // Resolved by walking the export table for the nearest exported address at or below the
+      // sampled instruction pointer -- no symbol server involved.
+      static constexpr size_t NumSyms = 24;
+      struct SymBucket { char name[64] = { }; uint64_t hits = 0; };
+      SymBucket syms[NumSyms] = { };
+      size_t symCount = 0;
+      HMODULE hotModule = nullptr;
+
+      const char* nearestExport(HMODULE mod, uintptr_t ip) {
+        auto base = reinterpret_cast<uint8_t*>(mod);
+        auto dos  = reinterpret_cast<IMAGE_DOS_HEADER*>(base);
+        auto nt   = reinterpret_cast<IMAGE_NT_HEADERS*>(base + dos->e_lfanew);
+        auto& dir = nt->OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_EXPORT];
+        if (dir.VirtualAddress == 0)
+          return nullptr;
+        auto exp = reinterpret_cast<IMAGE_EXPORT_DIRECTORY*>(base + dir.VirtualAddress);
+        auto funcs = reinterpret_cast<uint32_t*>(base + exp->AddressOfFunctions);
+        auto names = reinterpret_cast<uint32_t*>(base + exp->AddressOfNames);
+        auto ords  = reinterpret_cast<uint16_t*>(base + exp->AddressOfNameOrdinals);
+
+        uintptr_t bestAddr = 0;
+        const char* best = nullptr;
+        for (uint32_t i = 0; i < exp->NumberOfNames; i++) {
+          uintptr_t a = uintptr_t(base) + funcs[ords[i]];
+          if (a <= ip && a > bestAddr) {
+            bestAddr = a;
+            best = reinterpret_cast<const char*>(base + names[i]);
+          }
+        }
+        return best;
+      }
+
+      void countSym(const char* name) {
+        for (size_t i = 0; i < symCount; i++) {
+          if (std::strcmp(syms[i].name, name) == 0) {
+            syms[i].hits++;
+            return;
+          }
+        }
+        if (symCount < NumSyms) {
+          std::snprintf(syms[symCount].name, sizeof(syms[symCount].name), "%s", name);
+          syms[symCount].hits = 1;
+          symCount++;
+        }
+      }
+
+      static constexpr size_t NumOffsets = 16;
+      struct OffBucket { uintptr_t off = 0; uint64_t hits = 0; };
+      OffBucket offs[NumOffsets] = { };
+      size_t offCount = 0;
+
+      void countOffset(uintptr_t off) {
+        off &= ~uintptr_t(0x3F);              // 64-byte granularity: a function, not a line
+        for (size_t i = 0; i < offCount; i++) {
+          if (offs[i].off == off) {
+            offs[i].hits++;
+            return;
+          }
+        }
+        if (offCount < NumOffsets)
+          offs[offCount++] = { off, 1 };
+      }
+
+      void hit(uintptr_t ip) {
+        total++;
+        for (size_t i = 0; i < modCount; i++) {
+          if (ip >= mods[i].lo && ip < mods[i].hi) {
+            mods[i].hits++;
+            if (hotModule != nullptr && uintptr_t(hotModule) == mods[i].lo) {
+              const char* s = nearestExport(hotModule, ip);
+              countSym(s ? s : "<no-export>");
+              // Nearest-export naming is only as good as the export table: wine's ntdll has a
+              // great deal of unexported code, so an address in the wow64 transition path
+              // resolves to whichever export happens to sit below it. Raw offsets say which
+              // it really is -- compare them against the reported symbol's address.
+              countOffset(ip - mods[i].lo);
+            }
+            return;
+          }
+        }
+        unknown++;
+      }
+
+      void report() {
+        if (!file)
+          return;
+        std::fprintf(file, "total=%llu unknown=%llu", (unsigned long long) total,
+          (unsigned long long) unknown);
+        for (size_t i = 0; i < modCount; i++)
+          std::fprintf(file, " %s=%llu", mods[i].name, (unsigned long long) mods[i].hits);
+        for (size_t i = 0; i < symCount; i++)
+          std::fprintf(file, " |%s=%llu", syms[i].name, (unsigned long long) syms[i].hits);
+        for (size_t i = 0; i < offCount; i++)
+          std::fprintf(file, " @%llx=%llu", (unsigned long long) offs[i].off,
+            (unsigned long long) offs[i].hits);
+        // The reference point for reading those offsets.
+        if (hotModule != nullptr) {
+          void* nd = (void*) GetProcAddress(hotModule, "NtDelayExecution");
+          if (nd)
+            std::fprintf(file, " NtDelayExecution@%llx",
+              (unsigned long long) (uintptr_t(nd) - uintptr_t(hotModule)));
+        }
+        std::fprintf(file, "\n");
+        std::fflush(file);
+        for (size_t i = 0; i < modCount; i++)
+          mods[i].hits = 0;
+        symCount = 0;
+        offCount = 0;
+        unknown = total = 0;
+      }
+    };
+
+    static SampleProfiler g_profiler;
+
+    static DWORD WINAPI SampleThreadProc(void* param) {
+      HANDLE target = reinterpret_cast<HANDLE>(param);
+      auto tick = std::chrono::steady_clock::now();
+      while (!g_profiler.stop.load(std::memory_order_relaxed)) {
+        if (SuspendThread(target) != DWORD(-1)) {
+          CONTEXT ctx = { };
+          ctx.ContextFlags = CONTEXT_CONTROL;
+          if (GetThreadContext(target, &ctx)) {
+#if defined(_WIN64)
+            g_profiler.hit(uintptr_t(ctx.Rip));
+#else
+            g_profiler.hit(uintptr_t(ctx.Eip));
+#endif
+          }
+          ResumeThread(target);
+        }
+        Sleep(1);
+        auto now = std::chrono::steady_clock::now();
+        if (std::chrono::duration<double>(now - tick).count() >= 1.0) {
+          g_profiler.report();
+          tick = now;
+        }
+      }
+      return 0;
+    }
+
+    static void StartProfiler() {
+      if (g_profiler.thread != nullptr)
+        return;
+      const char* p = std::getenv("DXVK_SAMPLE_LOG");
+      if (p == nullptr || *p == '\0')
+        return;
+      // Only start once FFXiMain is mapped, so its address range is known.
+      if (GetModuleHandleA("FFXiMain.dll") == nullptr)
+        return;
+      g_profiler.file = std::fopen(p, "a");
+      if (g_profiler.file == nullptr)
+        return;
+      for (const char* m : { "FFXiMain.dll", "d3d8.dll", "d3d9.dll", "polcore.dll",
+                             "polcoreE.dll", "Ashita.dll", "kernel32.dll", "ntdll.dll",
+                             "user32.dll", "gdi32.dll", "ws2_32.dll", "msvcrt.dll",
+                             "horizon-loader.exe", "win32u.dll", "opengl32.dll" })
+        g_profiler.addModule(m);
+
+      // ntdll took ~90% of the frame thread's samples, so that is the module worth naming.
+      // Overridable, because the answer there may point at a different one next.
+      const char* hot = std::getenv("DXVK_SAMPLE_MODULE");
+      g_profiler.hotModule = GetModuleHandleA(hot && *hot ? hot : "ntdll.dll");
+
+      HANDLE dup = nullptr;
+      DuplicateHandle(GetCurrentProcess(), GetCurrentThread(),
+                      GetCurrentProcess(), &dup, 0, FALSE, DUPLICATE_SAME_ACCESS);
+      g_profiler.thread = CreateThread(nullptr, 0, &SampleThreadProc, dup, 0, nullptr);
+      Logger::info(str::format("sample profiler started over ", g_profiler.modCount, " modules"));
+    }
+
+    // The thunks. Each records elapsed time against its slot and forwards unchanged.
+    using PfnSleep       = void  (WINAPI*)(DWORD);
+    using PfnSleepEx     = DWORD (WINAPI*)(DWORD, BOOL);
+    using PfnWaitSingle  = DWORD (WINAPI*)(HANDLE, DWORD);
+    using PfnWaitSingleEx= DWORD (WINAPI*)(HANDLE, DWORD, BOOL);
+    using PfnWaitMulti   = DWORD (WINAPI*)(DWORD, const HANDLE*, BOOL, DWORD);
+    using PfnWaitMultiEx = DWORD (WINAPI*)(DWORD, const HANDLE*, BOOL, DWORD, BOOL);
+    using PfnWaitMsg     = DWORD (WINAPI*)(DWORD, const HANDLE*, BOOL, DWORD, DWORD);
+    using PfnGeneric     = int   (WINAPI*)(void);
+
+    static void*  g_orig[kNumWaitFns] = { };
+
+    // Short-sleep clamp (FFXI_SLEEP_CLAMP=<ms>, unset = leave alone).
+    //
+    // The client polls with ~200 Sleep() calls per frame. On Windows a 1 ms sleep costs about
+    // that; under this wine it costs 1.25 ms, and 200 of them is 250 ms of wall clock spent
+    // doing nothing -- which is the whole of the in-world stall. Requests at or below the clamp
+    // become Sleep(0), which yields the timeslice without arming a timer (measured at 4 us).
+    // The loop then makes progress at CPU speed. Only sleeps *shorter* than the clamp are
+    // touched, so any genuine long wait is left exactly as the client asked for it.
+    static DWORD g_sleepClamp = 0;
+
+    static void WINAPI HookSleep(DWORD ms) {
+      auto t0 = std::chrono::steady_clock::now();
+      reinterpret_cast<PfnSleep>(g_orig[kSleep])(ms <= g_sleepClamp ? 0 : ms);
+      g_waitProbe.add(kSleep, t0);
+    }
+    static DWORD WINAPI HookSleepEx(DWORD ms, BOOL alert) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnSleepEx>(g_orig[kSleepEx])(ms, alert);
+      g_waitProbe.add(kSleepEx, t0);
+      return r;
+    }
+    static DWORD WINAPI HookWaitSingle(HANDLE h, DWORD ms) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnWaitSingle>(g_orig[kWaitSingle])(h, ms);
+      g_waitProbe.add(kWaitSingle, t0);
+      return r;
+    }
+    static DWORD WINAPI HookWaitSingleEx(HANDLE h, DWORD ms, BOOL alert) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnWaitSingleEx>(g_orig[kWaitSingleEx])(h, ms, alert);
+      g_waitProbe.add(kWaitSingleEx, t0);
+      return r;
+    }
+    static DWORD WINAPI HookWaitMulti(DWORD n, const HANDLE* h, BOOL all, DWORD ms) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnWaitMulti>(g_orig[kWaitMulti])(n, h, all, ms);
+      g_waitProbe.add(kWaitMulti, t0);
+      return r;
+    }
+    static DWORD WINAPI HookWaitMultiEx(DWORD n, const HANDLE* h, BOOL all, DWORD ms, BOOL alert) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnWaitMultiEx>(g_orig[kWaitMultiEx])(n, h, all, ms, alert);
+      g_waitProbe.add(kWaitMultiEx, t0);
+      return r;
+    }
+    static DWORD WINAPI HookWaitMsg(DWORD n, const HANDLE* h, BOOL all, DWORD ms, DWORD mask) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD r = reinterpret_cast<PfnWaitMsg>(g_orig[kWaitMsg])(n, h, all, ms, mask);
+      g_waitProbe.add(kWaitMsg, t0);
+      return r;
+    }
+
+    // ws2_32 entry points, timed the same way. Their signatures differ but every one of them is
+    // called through a pointer whose arguments we never touch, so a cdecl-shaped forward with the
+    // right stack size is all that is needed -- declared with the real prototypes to keep the
+    // 32-bit stdcall stack balanced.
+    using PfnSelect   = int (WINAPI*)(int, void*, void*, void*, const void*);
+    using PfnRecv     = int (WINAPI*)(UINT_PTR, char*, int, int);
+    using PfnWSAWait  = DWORD (WINAPI*)(DWORD, const HANDLE*, BOOL, DWORD, BOOL);
+    using PfnWSAEnum  = int (WINAPI*)(UINT_PTR, HANDLE, void*);
+
+    static int WINAPI HookSelect(int n, void* r, void* w, void* e, const void* tv) {
+      auto t0 = std::chrono::steady_clock::now();
+      int rc = reinterpret_cast<PfnSelect>(g_orig[kSelect])(n, r, w, e, tv);
+      g_waitProbe.add(kSelect, t0);
+      return rc;
+    }
+    static int WINAPI HookRecv(UINT_PTR s, char* buf, int len, int flags) {
+      auto t0 = std::chrono::steady_clock::now();
+      int rc = reinterpret_cast<PfnRecv>(g_orig[kRecv])(s, buf, len, flags);
+      g_waitProbe.add(kRecv, t0);
+      return rc;
+    }
+    static DWORD WINAPI HookWSAWait(DWORD n, const HANDLE* h, BOOL all, DWORD ms, BOOL alert) {
+      auto t0 = std::chrono::steady_clock::now();
+      DWORD rc = reinterpret_cast<PfnWSAWait>(g_orig[kWSAWaitMulti])(n, h, all, ms, alert);
+      g_waitProbe.add(kWSAWaitMulti, t0);
+      return rc;
+    }
+    static int WINAPI HookWSAEnum(UINT_PTR s, HANDLE ev, void* out) {
+      auto t0 = std::chrono::steady_clock::now();
+      int rc = reinterpret_cast<PfnWSAEnum>(g_orig[kWSAEnumEvents])(s, ev, out);
+      g_waitProbe.add(kWSAEnumEvents, t0);
+      return rc;
+    }
+
+    struct WaitHookSpec { const char* dll; const char* name; void* thunk; };
+
+    // NtDelayExecution is where the sleeping actually shows up: 72% of the frame thread's
+    // samples. Sleep() in kernel32 forwards to it, but so does whatever else in the chain is
+    // pacing the client, which is why hooking Sleep alone changed nothing on the frame path.
+    // Interval is a LARGE_INTEGER in 100 ns units, negative meaning relative.
+    using PfnNtDelay = LONG (WINAPI*)(BOOLEAN, LARGE_INTEGER*);
+
+    static LONG WINAPI HookNtDelay(BOOLEAN alertable, LARGE_INTEGER* interval) {
+      auto t0 = std::chrono::steady_clock::now();
+      LARGE_INTEGER zero = { };
+      LARGE_INTEGER* use = interval;
+      if (g_sleepClamp != 0 && interval != nullptr && interval->QuadPart < 0) {
+        // Relative delay, in 100 ns units. Anything at or under the clamp becomes a plain
+        // yield, which costs microseconds instead of a timer arming at this wine's ~1.25 ms
+        // granularity.
+        int64_t ms = -interval->QuadPart / 10000;
+        if (ms <= int64_t(g_sleepClamp))
+          use = &zero;
+      }
+      LONG r = reinterpret_cast<PfnNtDelay>(g_orig[kNtDelay])(alertable, use);
+      g_waitProbe.add(kNtDelay, t0);
+      return r;
+    }
+
+    static const WaitHookSpec kWaitHooks[kNumWaitFns] = {
+      { "kernel32.dll", "Sleep",                     (void*) &HookSleep        },
+      { "kernel32.dll", "SleepEx",                   (void*) &HookSleepEx      },
+      { "kernel32.dll", "WaitForSingleObject",       (void*) &HookWaitSingle   },
+      { "kernel32.dll", "WaitForSingleObjectEx",     (void*) &HookWaitSingleEx },
+      { "kernel32.dll", "WaitForMultipleObjects",    (void*) &HookWaitMulti    },
+      { "kernel32.dll", "WaitForMultipleObjectsEx",  (void*) &HookWaitMultiEx  },
+      { "ws2_32.dll",   "select",                    (void*) &HookSelect       },
+      { "ws2_32.dll",   "recv",                      (void*) &HookRecv         },
+      { "ws2_32.dll",   "WSAWaitForMultipleEvents",  (void*) &HookWSAWait      },
+      { "ws2_32.dll",   "WSAEnumNetworkEvents",      (void*) &HookWSAEnum      },
+      { "user32.dll",   "MsgWaitForMultipleObjects", (void*) &HookWaitMsg      },
+      { "ntdll.dll",    "NtDelayExecution",          (void*) &HookNtDelay      },
+      // Timed in place rather than hooked -- it is our own function, not an import.
+      { "",             "",                          nullptr                   },
+    };
+
+    // Rewrite one module's import table. Walks every imported DLL's thunk array and swaps the
+    // resolved address for ours wherever the name matches, which is the least invasive way to
+    // intercept a call in a module we do not control -- no code is patched.
+    static uint32_t HookImportsOf(HMODULE mod) {
+      if (mod == nullptr)
+        return 0;
+      auto base = reinterpret_cast<uint8_t*>(mod);
+      auto dos  = reinterpret_cast<IMAGE_DOS_HEADER*>(base);
+      if (dos->e_magic != IMAGE_DOS_SIGNATURE)
+        return 0;
+      auto nt = reinterpret_cast<IMAGE_NT_HEADERS*>(base + dos->e_lfanew);
+      if (nt->Signature != IMAGE_NT_SIGNATURE)
+        return 0;
+      auto& dir = nt->OptionalHeader.DataDirectory[IMAGE_DIRECTORY_ENTRY_IMPORT];
+      if (dir.VirtualAddress == 0)
+        return 0;
+
+      uint32_t patched = 0;
+      auto desc = reinterpret_cast<IMAGE_IMPORT_DESCRIPTOR*>(base + dir.VirtualAddress);
+      for (; desc->Name; desc++) {
+        const char* dllName = reinterpret_cast<const char*>(base + desc->Name);
+        if (desc->OriginalFirstThunk == 0 || desc->FirstThunk == 0)
+          continue;
+        auto names = reinterpret_cast<IMAGE_THUNK_DATA*>(base + desc->OriginalFirstThunk);
+        auto addrs = reinterpret_cast<IMAGE_THUNK_DATA*>(base + desc->FirstThunk);
+
+        for (; names->u1.AddressOfData; names++, addrs++) {
+          if (names->u1.Ordinal & IMAGE_ORDINAL_FLAG)
+            continue;
+          auto imp = reinterpret_cast<IMAGE_IMPORT_BY_NAME*>(base + names->u1.AddressOfData);
+
+          for (size_t i = 0; i < kNumWaitFns; i++) {
+            if (std::strcmp(reinterpret_cast<const char*>(imp->Name), kWaitHooks[i].name) != 0)
+              continue;
+            if (_stricmp(dllName, kWaitHooks[i].dll) != 0)
+              continue;
+            if (g_orig[i] == nullptr)
+              g_orig[i] = reinterpret_cast<void*>(addrs->u1.Function);
+
+            DWORD old = 0;
+            if (VirtualProtect(&addrs->u1.Function, sizeof(void*), PAGE_READWRITE, &old)) {
+              addrs->u1.Function = reinterpret_cast<uintptr_t>(kWaitHooks[i].thunk);
+              VirtualProtect(&addrs->u1.Function, sizeof(void*), old, &old);
+              patched++;
+            }
+          }
+        }
+      }
+      return patched;
+    }
+
+    // The client's own module is the target; Ashita's proxy and the loader are hooked too so a
+    // wait made on the client's behalf is not missed.
+    static void InstallWaitHooks() {
+      static bool clampRead = false;
+      if (!clampRead) {
+        clampRead = true;
+        const char* v = std::getenv("FFXI_SLEEP_CLAMP");
+        if (v && *v)
+          g_sleepClamp = DWORD(std::atoi(v));
+      }
+      // The hooks carry both the probe and the clamp, so either one being asked for installs
+      // them.
+      if (g_waitProbe.hooked || (!g_waitProbe.on() && g_sleepClamp == 0))
+        return;
+      // Every loaded module, not a hand-picked list. The frame thread's sleeping did not go
+      // through FFXiMain's own kernel32 imports, so restricting the sweep to the obvious
+      // modules silently measured -- and clamped -- nothing on the path that matters.
+      uint32_t patched = 0;
+      HANDLE snap = CreateToolhelp32Snapshot(TH32CS_SNAPMODULE, 0);
+      if (snap != INVALID_HANDLE_VALUE) {
+        MODULEENTRY32 me = { };
+        me.dwSize = sizeof(me);
+        for (BOOL ok = Module32First(snap, &me); ok; ok = Module32Next(snap, &me)) {
+          // Our own module is included on purpose. The frame thread's NtDelayExecution calls
+          // showed up in no module's import table, and DXVK -- which runs on that very thread
+          // -- was the one module being skipped.
+          patched += HookImportsOf(me.hModule);
+        }
+        CloseHandle(snap);
+      }
+      if (patched > 0) {
+        g_waitProbe.hooked = true;
+        Logger::info(str::format("wait probe: patched ", patched, " import slots"));
+      }
+    }
+  }
+
+  // Windows timer resolution (DXVK_TIMER_RESOLUTION=<ms>, 0 or unset = leave alone).
+  //
+  // A game that paces itself with Sleep() gets whatever granularity the process has asked for.
+  // The default is coarse -- ~15.6 ms -- and a loop that sleeps several times per frame then
+  // quantises to a multiple of that. FFXI in-world was measured idling at 11-13% CPU while
+  // stalling ~120 ms per frame, which is what eight coarse sleeps look like. timeBeginPeriod
+  // asks for a finer timer, process-wide, for as long as we are loaded. Resolved dynamically so
+  // DXVK does not gain a winmm link dependency.
+  namespace {
+    void ApplyTimerResolution() {
+      const char* v = std::getenv("DXVK_TIMER_RESOLUTION");
+      if (v == nullptr || *v == '\0')
+        return;
+      UINT ms = UINT(std::atoi(v));
+      if (ms == 0)
+        return;
+      HMODULE winmm = LoadLibraryA("winmm.dll");
+      if (winmm == nullptr)
+        return;
+      using PFN_timeBeginPeriod = MMRESULT (WINAPI *)(UINT);
+      auto fn = reinterpret_cast<PFN_timeBeginPeriod>(
+        reinterpret_cast<void*>(GetProcAddress(winmm, "timeBeginPeriod")));
+      if (fn != nullptr && fn(ms) == 0)
+        Logger::info(str::format("timer resolution set to ", ms, " ms"));
+      else
+        Logger::warn("timeBeginPeriod failed");
+    }
+  }
+
+  // FFXI frame-rate divisor (FFXI_FPS_DIVISOR=<n>, 1 = 60fps target, 2 = 30fps, 0 = leave alone).
+  //
+  // The retail client limits itself to 60/n frames per second. On this stack the limiter is the
+  // binding constraint long before the renderer is, so the cap has to be measurable and
+  // adjustable to tell "we are slow" apart from "the client is waiting on purpose". Same
+  // signature and pointer walk Ashita's own `fps` addon uses; done here because Ashita's plugin
+  // host does not load in this install. No effect unless the environment variable is set.
+  namespace {
+    bool ApplyFfxiFpsDivisor() {
+      const char* v = std::getenv("FFXI_FPS_DIVISOR");
+      if (v == nullptr || *v == '\0')
+        return true;
+      uint32_t divisor = uint32_t(std::atoi(v));
+      if (divisor == 0)
+        return true;
+
+      HMODULE mod = GetModuleHandleA("FFXiMain.dll");
+      if (mod == nullptr)
+        return false;
+
+      auto base = reinterpret_cast<const uint8_t*>(mod);
+      auto dos  = reinterpret_cast<const IMAGE_DOS_HEADER*>(base);
+      auto nt   = reinterpret_cast<const IMAGE_NT_HEADERS*>(base + dos->e_lfanew);
+      size_t size = nt->OptionalHeader.SizeOfImage;
+
+      static const uint8_t sig[] = {
+        0x81, 0xEC, 0x00, 0x01, 0x00, 0x00, 0x3B, 0xC1, 0x74, 0x21, 0x8B, 0x0D };
+
+      for (size_t i = 0; i + sizeof(sig) < size; i++) {
+        if (std::memcmp(base + i, sig, sizeof(sig)) != 0)
+          continue;
+
+        auto slot = *reinterpret_cast<const uint32_t* const*>(base + i + 0x0C);
+        MEMORY_BASIC_INFORMATION mbi = { };
+        if (!VirtualQuery(slot, &mbi, sizeof(mbi)) || mbi.State != MEM_COMMIT)
+          return false;
+
+        auto obj = reinterpret_cast<uint8_t*>(uintptr_t(*slot));
+        if (!VirtualQuery(obj, &mbi, sizeof(mbi)) || mbi.State != MEM_COMMIT)
+          return false;
+
+        DWORD old = 0;
+        if (VirtualProtect(obj + 0x30, sizeof(uint32_t), PAGE_READWRITE, &old)) {
+          *reinterpret_cast<uint32_t*>(obj + 0x30) = divisor;
+          VirtualProtect(obj + 0x30, sizeof(uint32_t), old, &old);
+          Logger::info(str::format("FFXI frame divisor set to ", divisor));
+          return true;
+        }
+        return false;
+      }
+      return false;
+    }
+  }
+
   HRESULT STDMETHODCALLTYPE D3D9SwapChainEx::Present(
     const RECT*    pSourceRect,
     const RECT*    pDestRect,
           HWND     hDestWindowOverride,
     const RGNDATA* pDirtyRegion,
           DWORD    dwFlags) {
+    // The divisor lives in an object the client allocates during start-up, so this is retried
+    // on early frames rather than done once at device creation.
+    static bool timerDone = false;
+    if (!timerDone) {
+      timerDone = true;
+      ApplyTimerResolution();
+    }
+
+    static bool divisorDone = false;
+    static uint32_t divisorAttempts = 0;
+    if (!divisorDone && divisorAttempts < 600) {
+      divisorAttempts++;
+      divisorDone = ApplyFfxiFpsDivisor();
+    }
+
+    // Retried like the divisor: FFXiMain.dll is not mapped yet on the first frames.
+    static uint32_t waitHookAttempts = 0;
+    if (waitHookAttempts < 600) {
+      waitHookAttempts++;
+      InstallWaitHooks();
+    }
+    g_waitProbe.presentThread.store(GetCurrentThreadId(), std::memory_order_relaxed);
+    static uint32_t profilerAttempts = 0;
+    if (g_profiler.thread == nullptr && profilerAttempts < 600) {
+      profilerAttempts++;
+      StartProfiler();
+    }
+    g_waitProbe.frame();
+
+    auto probeEnter = std::chrono::steady_clock::now();
+    g_presentProbe.enter(probeEnter);
+    struct ProbeExit {
+      std::chrono::steady_clock::time_point t;
+      ~ProbeExit() { g_presentProbe.exit(t); }
+    } probeExit { probeEnter };
+
     D3D9DeviceLock lock = m_parent->LockDevice();
 
     uint32_t presentInterval = m_presentParams.PresentationInterval;
@@ -524,6 +1249,22 @@ namespace dxvk {
     if (FAILED(this->GetDisplayModeEx(&mode, nullptr)))
       return D3DERR_INVALIDCALL;
 
+    // A 2002 engine may poll this to pace itself. If wine reports a 0 Hz refresh rate the
+    // maths below divides by zero, and any caller waiting for a vblank waits forever. Report
+    // what we were told, once, and refuse to trust a nonsense value.
+    static bool loggedMode = false;
+    if (!loggedMode) {
+      loggedMode = true;
+      Logger::info(str::format("GetRasterStatus: display mode ", mode.Width, "x", mode.Height,
+                               " @ ", mode.RefreshRate, " Hz"));
+    }
+    if (mode.RefreshRate == 0)
+      mode.RefreshRate = 60;
+
+    static uint64_t calls = 0;
+    if ((++calls % 5000) == 0)
+      Logger::info(str::format("GetRasterStatus called ", calls, " times"));
+
     uint32_t scanLineCount = mode.Height + vBlankLineCount;
 
     auto nowUs = std::chrono::time_point_cast<std::chrono::microseconds>(
@@ -1091,8 +1832,15 @@ namespace dxvk {
 
 
   void D3D9SwapChainEx::SyncFrameLatency() {
-    // Wait for the sync event so that we respect the maximum frame latency
+    // Wait for the sync event so that we respect the maximum frame latency.
+    //
+    // This is the one place the frame thread blocks, and its wait resolves inside ntdll --
+    // which is exactly where profiling put ~70-95% of the thread's samples, with no import
+    // table involved. Timed here so "the frame thread is idle" can be attributed to waiting
+    // on the GPU rather than left as a mystery.
+    auto t0 = std::chrono::steady_clock::now();
     m_frameLatencySignal->wait(m_frameId - GetActualFrameLatency());
+    g_waitProbe.add(kFrameLatency, t0);
   }
 
 
diff --git a/src/d3d9/d3d9_swapchain.h b/src/d3d9/d3d9_swapchain.h
index 150578c..5bdacb6 100644
--- a/src/d3d9/d3d9_swapchain.h
+++ b/src/d3d9/d3d9_swapchain.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device_child.h"
 #include "d3d9_device.h"
 #include "d3d9_format.h"
diff --git a/src/d3d9/d3d9_swvp_emu.h b/src/d3d9/d3d9_swvp_emu.h
index 91aae4c..8d521b8 100644
--- a/src/d3d9/d3d9_swvp_emu.h
+++ b/src/d3d9/d3d9_swvp_emu.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <unordered_map>
 
 #include "d3d9_include.h"
diff --git a/src/d3d9/d3d9_texture.h b/src/d3d9/d3d9_texture.h
index 317f72e..f54cb82 100644
--- a/src/d3d9/d3d9_texture.h
+++ b/src/d3d9/d3d9_texture.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device.h"
 #include "d3d9_surface.h"
 #include "d3d9_volume.h"
diff --git a/src/d3d9/d3d9_util.h b/src/d3d9/d3d9_util.h
index 5899cd4..fea5fb1 100644
--- a/src/d3d9/d3d9_util.h
+++ b/src/d3d9/d3d9_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #define D3D11_NO_HELPERS
 
 #include "d3d9_include.h"
diff --git a/src/d3d9/d3d9_vertex_declaration.h b/src/d3d9/d3d9_vertex_declaration.h
index 240aa6e..6ad76e1 100644
--- a/src/d3d9/d3d9_vertex_declaration.h
+++ b/src/d3d9/d3d9_vertex_declaration.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "d3d9_device_child.h"
 #include "d3d9_util.h"
 
diff --git a/src/dxbc/dxbc_analysis.h b/src/dxbc/dxbc_analysis.h
index b40cc94..a168f7e 100644
--- a/src/dxbc/dxbc_analysis.h
+++ b/src/dxbc/dxbc_analysis.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_chunk_isgn.h"
 #include "dxbc_decoder.h"
 #include "dxbc_defs.h"
diff --git a/src/dxbc/dxbc_chunk_isgn.h b/src/dxbc/dxbc_chunk_isgn.h
index 030372f..b57bc75 100644
--- a/src/dxbc/dxbc_chunk_isgn.h
+++ b/src/dxbc/dxbc_chunk_isgn.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <cctype>
 
 #include "dxbc_common.h"
diff --git a/src/dxbc/dxbc_chunk_shex.h b/src/dxbc/dxbc_chunk_shex.h
index 8deecc8..fe17ea7 100644
--- a/src/dxbc/dxbc_chunk_shex.h
+++ b/src/dxbc/dxbc_chunk_shex.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_common.h"
 #include "dxbc_decoder.h"
 #include "dxbc_reader.h"
diff --git a/src/dxbc/dxbc_common.h b/src/dxbc/dxbc_common.h
index 9c490d6..d445d6b 100644
--- a/src/dxbc/dxbc_common.h
+++ b/src/dxbc/dxbc_common.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_include.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_compiler.h b/src/dxbc/dxbc_compiler.h
index 2216642..85a6123 100644
--- a/src/dxbc/dxbc_compiler.h
+++ b/src/dxbc/dxbc_compiler.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <array>
 #include <vector>
 
diff --git a/src/dxbc/dxbc_decoder.h b/src/dxbc/dxbc_decoder.h
index 72b4e96..65fa01a 100644
--- a/src/dxbc/dxbc_decoder.h
+++ b/src/dxbc/dxbc_decoder.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <array>
 
 #include "dxbc_common.h"
diff --git a/src/dxbc/dxbc_defs.h b/src/dxbc/dxbc_defs.h
index decd767..3ced0bb 100644
--- a/src/dxbc/dxbc_defs.h
+++ b/src/dxbc/dxbc_defs.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_enums.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_enums.h b/src/dxbc/dxbc_enums.h
index 214780d..2687930 100644
--- a/src/dxbc/dxbc_enums.h
+++ b/src/dxbc/dxbc_enums.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_include.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_header.h b/src/dxbc/dxbc_header.h
index 8eca0d4..798a41a 100644
--- a/src/dxbc/dxbc_header.h
+++ b/src/dxbc/dxbc_header.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "dxbc_reader.h"
diff --git a/src/dxbc/dxbc_modinfo.h b/src/dxbc/dxbc_modinfo.h
index 13e733d..8b9e091 100644
--- a/src/dxbc/dxbc_modinfo.h
+++ b/src/dxbc/dxbc_modinfo.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_options.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_options.h b/src/dxbc/dxbc_options.h
index d21f3b1..ad74f00 100644
--- a/src/dxbc/dxbc_options.h
+++ b/src/dxbc/dxbc_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_reader.h b/src/dxbc/dxbc_reader.h
index a160001..5184f55 100644
--- a/src/dxbc/dxbc_reader.h
+++ b/src/dxbc/dxbc_reader.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <cstddef>
 #include <string>
 
diff --git a/src/dxbc/dxbc_tag.h b/src/dxbc/dxbc_tag.h
index 2ba1750..3168b57 100644
--- a/src/dxbc/dxbc_tag.h
+++ b/src/dxbc/dxbc_tag.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_include.h"
 
 namespace dxvk {
diff --git a/src/dxbc/dxbc_util.h b/src/dxbc/dxbc_util.h
index 4bc4962..766bae0 100644
--- a/src/dxbc/dxbc_util.h
+++ b/src/dxbc/dxbc_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxbc_common.h"
 #include "dxbc_enums.h"
 
diff --git a/src/dxgi/dxgi_format.h b/src/dxgi/dxgi_format.h
index 59e6493..4d1a573 100644
--- a/src/dxgi/dxgi_format.h
+++ b/src/dxgi/dxgi_format.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxgi_include.h"
 
 #include "../dxvk/dxvk_adapter.h"
diff --git a/src/dxgi/dxgi_interfaces.h b/src/dxgi/dxgi_interfaces.h
index f7a9395..e9abce7 100644
--- a/src/dxgi/dxgi_interfaces.h
+++ b/src/dxgi/dxgi_interfaces.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_include.h"
 
 #include "dxgi_format.h"
diff --git a/src/dxgi/dxgi_monitor.h b/src/dxgi/dxgi_monitor.h
index 0c70101..6afd63c 100644
--- a/src/dxgi/dxgi_monitor.h
+++ b/src/dxgi/dxgi_monitor.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/dxgi/dxgi_options.h b/src/dxgi/dxgi_options.h
index 8eb19d0..40454c6 100644
--- a/src/dxgi/dxgi_options.h
+++ b/src/dxgi/dxgi_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/config/config.h"
 
 #include "../dxvk/dxvk_include.h"
diff --git a/src/dxgi/dxgi_output.h b/src/dxgi/dxgi_output.h
index 0dc3086..d1b93ae 100644
--- a/src/dxgi/dxgi_output.h
+++ b/src/dxgi/dxgi_output.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxgi_monitor.h"
 #include "dxgi_object.h"
 
diff --git a/src/dxso/dxso_analysis.h b/src/dxso/dxso_analysis.h
index c1321e0..e52cb03 100644
--- a/src/dxso/dxso_analysis.h
+++ b/src/dxso/dxso_analysis.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_modinfo.h"
 #include "dxso_decoder.h"
 
diff --git a/src/dxso/dxso_compiler.h b/src/dxso/dxso_compiler.h
index f26732f..33e065d 100644
--- a/src/dxso/dxso_compiler.h
+++ b/src/dxso/dxso_compiler.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_decoder.h"
 #include "dxso_header.h"
 #include "dxso_modinfo.h"
diff --git a/src/dxso/dxso_ctab.h b/src/dxso/dxso_ctab.h
index 56f4f94..cd8601c 100644
--- a/src/dxso/dxso_ctab.h
+++ b/src/dxso/dxso_ctab.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_common.h"
 
 #include "dxso_reader.h"
diff --git a/src/dxso/dxso_decoder.h b/src/dxso/dxso_decoder.h
index 178f807..132632e 100644
--- a/src/dxso/dxso_decoder.h
+++ b/src/dxso/dxso_decoder.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_common.h"
 #include "dxso_enums.h"
 #include "dxso_code.h"
diff --git a/src/dxso/dxso_isgn.h b/src/dxso/dxso_isgn.h
index 2faa04e..ade58a8 100644
--- a/src/dxso/dxso_isgn.h
+++ b/src/dxso/dxso_isgn.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_decoder.h"
 
 namespace dxvk {
diff --git a/src/dxso/dxso_module.h b/src/dxso/dxso_module.h
index 4b8238d..354e630 100644
--- a/src/dxso/dxso_module.h
+++ b/src/dxso/dxso_module.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_reader.h"
 #include "dxso_code.h"
 #include "dxso_header.h"
diff --git a/src/dxso/dxso_options.h b/src/dxso/dxso_options.h
index c977ce8..bd39cc9 100644
--- a/src/dxso/dxso_options.h
+++ b/src/dxso/dxso_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 #include "../d3d9/d3d9_options.h"
 
diff --git a/src/dxso/dxso_tables.h b/src/dxso/dxso_tables.h
index 73e3801..7c8a1da 100644
--- a/src/dxso/dxso_tables.h
+++ b/src/dxso/dxso_tables.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxso_enums.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_adapter.h b/src/dxvk/dxvk_adapter.h
index f2fb1f0..67fe43d 100644
--- a/src/dxvk/dxvk_adapter.h
+++ b/src/dxvk/dxvk_adapter.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_device_info.h"
 #include "dxvk_extensions.h"
 #include "dxvk_include.h"
diff --git a/src/dxvk/dxvk_barrier.h b/src/dxvk/dxvk_barrier.h
index f7a7713..405add3 100644
--- a/src/dxvk/dxvk_barrier.h
+++ b/src/dxvk/dxvk_barrier.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <utility>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_bind_mask.h b/src/dxvk/dxvk_bind_mask.h
index b45b9b6..a09118d 100644
--- a/src/dxvk/dxvk_bind_mask.h
+++ b/src/dxvk/dxvk_bind_mask.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_buffer.h"
 #include "dxvk_descriptor.h"
 #include "dxvk_image.h"
diff --git a/src/dxvk/dxvk_buffer.h b/src/dxvk/dxvk_buffer.h
index 4d94cec..a5c23d8 100644
--- a/src/dxvk/dxvk_buffer.h
+++ b/src/dxvk/dxvk_buffer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <unordered_map>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_cmdlist.cpp b/src/dxvk/dxvk_cmdlist.cpp
index 565b9a7..47ec237 100644
--- a/src/dxvk/dxvk_cmdlist.cpp
+++ b/src/dxvk/dxvk_cmdlist.cpp
@@ -125,6 +125,12 @@ namespace dxvk {
   }
   
   
+  VkResult DxvkCommandList::synchronizeBounded(uint64_t timeoutNs) {
+    return m_vkd->vkWaitForFences(
+      m_vkd->device(), 1, &m_fence, VK_FALSE, timeoutNs);
+  }
+
+
   VkResult DxvkCommandList::synchronize() {
     VkResult status = VK_TIMEOUT;
     
diff --git a/src/dxvk/dxvk_cmdlist.h b/src/dxvk/dxvk_cmdlist.h
index 02549df..28b1b5a 100644
--- a/src/dxvk/dxvk_cmdlist.h
+++ b/src/dxvk/dxvk_cmdlist.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <limits>
 
 #include "dxvk_bind_mask.h"
@@ -103,6 +106,12 @@ namespace dxvk {
      * \returns Synchronization status
      */
     VkResult synchronize();
+
+    /**
+     * \brief Waits for fence with a bounded timeout (ns). VK_TIMEOUT on expiry.
+     * Used by the small-render-target readback fast path (D3D9_RT_READBACK_FENCE).
+     */
+    VkResult synchronizeBounded(uint64_t timeoutNs);
     
     /**
      * \brief Stat counters
diff --git a/src/dxvk/dxvk_compute.h b/src/dxvk/dxvk_compute.h
index 186d17e..9820956 100644
--- a/src/dxvk/dxvk_compute.h
+++ b/src/dxvk/dxvk_compute.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "../util/sync/sync_list.h"
diff --git a/src/dxvk/dxvk_constant_state.h b/src/dxvk/dxvk_constant_state.h
index b17dd9b..8035d6a 100644
--- a/src/dxvk/dxvk_constant_state.h
+++ b/src/dxvk/dxvk_constant_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_buffer.h"
 #include "dxvk_framebuffer.h"
 #include "dxvk_limits.h"
diff --git a/src/dxvk/dxvk_context.cpp b/src/dxvk/dxvk_context.cpp
index 6d30088..1cb3822 100644
--- a/src/dxvk/dxvk_context.cpp
+++ b/src/dxvk/dxvk_context.cpp
@@ -1,4 +1,9 @@
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
 #include <cstring>
+#include <map>
+#include <string>
 #include <vector>
 #include <utility>
 
@@ -6,7 +11,49 @@
 #include "dxvk_context.h"
 
 namespace dxvk {
-  
+
+  // Render-pass-break histogram (DXVK_PASS_PROBE=<path>).
+  //
+  // On Metal every render pass is a separate MTLRenderCommandEncoder, so a frame that spills
+  // its render pass 60 times pays for 60 encoders. This records which DxvkContext operation
+  // forced each break, so the expensive ones can be found rather than guessed at.
+  class DxvkPassProbe {
+  public:
+    static DxvkPassProbe& get() {
+      static DxvkPassProbe instance;
+      return instance;
+    }
+
+    void spill(const char* why) {
+      if (m_file == nullptr)
+        return;
+      m_counts[why] += 1;
+      m_total += 1;
+      auto now = std::chrono::steady_clock::now();
+      if (std::chrono::duration<double>(now - m_tick).count() < 5.0)
+        return;
+      m_tick = now;
+      std::fprintf(m_file, "--- %llu spills total\n", (unsigned long long) m_total);
+      for (const auto& e : m_counts)
+        std::fprintf(m_file, "%8llu  %s\n", (unsigned long long) e.second, e.first.c_str());
+      std::fflush(m_file);
+    }
+
+  private:
+    DxvkPassProbe() {
+      const char* path = std::getenv("DXVK_PASS_PROBE");
+      if (path != nullptr && *path != '\0')
+        m_file = std::fopen(path, "a");
+      m_tick = std::chrono::steady_clock::now();
+    }
+
+    FILE* m_file = nullptr;
+    uint64_t m_total = 0;
+    std::map<std::string, uint64_t> m_counts;
+    std::chrono::steady_clock::time_point m_tick;
+  };
+
+
   DxvkContext::DxvkContext(const Rc<DxvkDevice>& device)
   : m_device      (device),
     m_common      (&device->m_objects),
@@ -535,15 +582,43 @@ namespace dxvk {
     auto srcSlice = srcBuffer->getSliceHandle(srcOffset, numBytes);
     auto dstSlice = dstBuffer->getSliceHandle(dstOffset, numBytes);
 
+    // Schedule partial uploads early too (DXVK_EARLY_BUFFER_COPY=1).
+    //
+    // The discard path above only applies when the copy covers a whole buffer of at most 256 KB.
+    // FFXI's uploads are partial dirty ranges of larger buffers, so it always missed, and every
+    // one of them ended the render pass: this scene spilled 604,924 times in a 40 s run, with
+    // copyBuffer the single largest cause -- more render-pass breaks per frame than draws, which
+    // is why the GPU sits at ~10% while the frame takes 40 ms.
+    //
+    // The copy can be moved to the init command buffer, which runs before the exec buffer in the
+    // same submission, whenever nothing already recorded touches either slice: the barrier set
+    // knows every access recorded so far this submission, so if neither slice is dirty there is
+    // no recorded read or write for the copy to be reordered ahead of. Off by default until it
+    // has been measured against a scene, because getting this wrong shows up as corrupt geometry
+    // rather than as a crash.
+    static const bool earlyCopyEnabled = [] {
+      const char* v = std::getenv("DXVK_EARLY_BUFFER_COPY");
+      return v && *v && *v != '0';
+    }();
+
+    bool earlyCopy = false;
+
     if (!replaceBuffer) {
-      this->spillRenderPass(true);
+      bool srcDirty = m_execBarriers.isBufferDirty(srcSlice, DxvkAccess::Read);
+      bool dstDirty = m_execBarriers.isBufferDirty(dstSlice, DxvkAccess::Write)
+                   || m_execBarriers.isBufferDirty(dstSlice, DxvkAccess::Read);
 
-      if (m_execBarriers.isBufferDirty(srcSlice, DxvkAccess::Read)
-       || m_execBarriers.isBufferDirty(dstSlice, DxvkAccess::Write))
-        m_execBarriers.recordCommands(m_cmd);
+      earlyCopy = earlyCopyEnabled && srcIsReadOnly && !srcDirty && !dstDirty;
+
+      if (!earlyCopy) {
+        this->spillRenderPass(true);
+
+        if (srcDirty || m_execBarriers.isBufferDirty(dstSlice, DxvkAccess::Write))
+          m_execBarriers.recordCommands(m_cmd);
+      }
     }
 
-    DxvkCmdBuffer cmdBuffer = replaceBuffer
+    DxvkCmdBuffer cmdBuffer = (replaceBuffer || earlyCopy)
       ? DxvkCmdBuffer::InitBuffer
       : DxvkCmdBuffer::ExecBuffer;
 
@@ -555,7 +630,7 @@ namespace dxvk {
     m_cmd->cmdCopyBuffer(cmdBuffer,
       srcSlice.handle, dstSlice.handle, 1, &bufferRegion);
 
-    auto& barriers = replaceBuffer
+    auto& barriers = (replaceBuffer || earlyCopy)
       ? m_initBarriers
       : m_execBarriers;
 
@@ -3863,10 +3938,12 @@ namespace dxvk {
   }
   
   
-  void DxvkContext::spillRenderPass(bool suspend) {
+  void DxvkContext::spillRenderPass(bool suspend, const char* why) {
     if (m_flags.test(DxvkContextFlag::GpRenderPassBound)) {
       m_flags.clr(DxvkContextFlag::GpRenderPassBound);
 
+      DxvkPassProbe::get().spill(why);
+
       this->pauseTransformFeedback();
       
       m_queryManager.endQueries(m_cmd, VK_QUERY_TYPE_OCCLUSION);
diff --git a/src/dxvk/dxvk_context.h b/src/dxvk/dxvk_context.h
index c5547c1..a9ddc19 100644
--- a/src/dxvk/dxvk_context.h
+++ b/src/dxvk/dxvk_context.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_barrier.h"
 #include "dxvk_bind_mask.h"
 #include "dxvk_cmdlist.h"
@@ -1207,7 +1210,9 @@ namespace dxvk {
     void flushSharedImages();
 
     void startRenderPass();
-    void spillRenderPass(bool suspend);
+    // The default argument resolves at the call site, so every spill records which operation
+    // forced it. Used by the DXVK_PASS_PROBE histogram; costs nothing when the probe is off.
+    void spillRenderPass(bool suspend, const char* why = __builtin_FUNCTION());
     
     void renderPassBindFramebuffer(
       const DxvkFramebufferInfo&  framebufferInfo,
diff --git a/src/dxvk/dxvk_context_state.h b/src/dxvk/dxvk_context_state.h
index 27a6de5..f98f4c5 100644
--- a/src/dxvk/dxvk_context_state.h
+++ b/src/dxvk/dxvk_context_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_buffer.h"
 #include "dxvk_compute.h"
 #include "dxvk_constant_state.h"
diff --git a/src/dxvk/dxvk_cs.h b/src/dxvk/dxvk_cs.h
index ab2733c..5ec525e 100644
--- a/src/dxvk/dxvk_cs.h
+++ b/src/dxvk/dxvk_cs.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 #include <condition_variable>
 #include <mutex>
diff --git a/src/dxvk/dxvk_data.h b/src/dxvk/dxvk_data.h
index 2a5ee92..e3b6e5f 100644
--- a/src/dxvk/dxvk_data.h
+++ b/src/dxvk/dxvk_data.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_device.cpp b/src/dxvk/dxvk_device.cpp
index dc12ab4..46caf0e 100644
--- a/src/dxvk/dxvk_device.cpp
+++ b/src/dxvk/dxvk_device.cpp
@@ -1,8 +1,94 @@
 #include "dxvk_device.h"
 #include "dxvk_instance.h"
 
+#include <chrono>
+#include <cstdio>
+#include <cstdlib>
+#include <ctime>
+
 namespace dxvk {
-  
+
+  // Frame-rate logger (DXVK_FPS_LOG=<path>). Writes one CSV row per second with the per-frame
+  // work that actually costs CPU on Metal: draws, render passes (each one is a new
+  // MTLRenderCommandEncoder), barriers, queue submits and CS chunks, plus how long the GPU sat
+  // idle. A number that does not depend on reading an on-screen overlay, so every renderer
+  // configuration can be compared with the same instrument. Off unless the env var is set.
+  struct DxvkFpsLog {
+    static constexpr size_t NumTracked = 10;
+    static constexpr DxvkStatCounter Tracked[NumTracked] = {
+      DxvkStatCounter::CmdDrawCalls,
+      DxvkStatCounter::CmdRenderPassCount,
+      DxvkStatCounter::CmdBarrierCount,
+      DxvkStatCounter::QueueSubmitCount,
+      DxvkStatCounter::CsChunkCount,
+      DxvkStatCounter::GpuIdleTicks,
+      // How long the CPU spends *blocked on the GPU* inside DxvkDevice::waitForResource --
+      // i.e. the game locked a buffer or texture the GPU was still reading, so the queue had to
+      // be flushed and waited on. The HUD shows these too, but only as the worst single frame
+      // in each refresh interval, which reads far more alarming than the average. Logging both
+      // the count and the time per frame makes the average measurable.
+      DxvkStatCounter::GpuSyncCount,
+      DxvkStatCounter::GpuSyncTicks,
+      // ...and the same split by what was locked. Buffer stalls and texture stalls have
+      // completely different fixes, so the totals alone are not actionable.
+      DxvkStatCounter::GpuSyncImageCount,
+      DxvkStatCounter::GpuSyncImageTicks,
+    };
+
+    FILE* file = nullptr;
+    std::chrono::steady_clock::time_point t0;
+    std::chrono::steady_clock::time_point tick;
+    uint64_t frames = 0;
+    uint64_t prev[NumTracked] = { };
+
+    DxvkFpsLog() {
+      const char* path = std::getenv("DXVK_FPS_LOG");
+      if (path == nullptr || *path == '\0')
+        return;
+      file = std::fopen(path, "a");
+      if (file == nullptr)
+        return;
+      t0 = tick = std::chrono::steady_clock::now();
+      std::fprintf(file,
+        "epoch,elapsed,fps,draws,passes,barriers,submits,cschunks,gpuidle_us,"
+        "syncs,sync_ms,imgsyncs,imgsync_ms\n");
+      std::fflush(file);
+    }
+
+    void frame(const DxvkStatCounters& ctrs) {
+      if (file == nullptr)
+        return;
+      frames += 1;
+      auto now = std::chrono::steady_clock::now();
+      double dt = std::chrono::duration<double>(now - tick).count();
+      if (dt < 1.0)
+        return;
+
+      double perFrame[NumTracked];
+      for (size_t i = 0; i < NumTracked; i++) {
+        uint64_t cur = ctrs.getCtr(Tracked[i]);
+        perFrame[i] = double(cur - prev[i]) / double(frames);
+        prev[i] = cur;
+      }
+
+      // GpuSyncTicks accumulates microseconds; report it as milliseconds per frame, which is
+      // directly comparable with the frame time.
+      std::fprintf(file, "%lld,%.1f,%.2f,%.1f,%.1f,%.1f,%.2f,%.1f,%.0f,%.2f,%.3f,%.2f,%.3f\n",
+        (long long) std::time(nullptr),
+        std::chrono::duration<double>(now - t0).count(),
+        double(frames) / dt,
+        perFrame[0], perFrame[1], perFrame[2], perFrame[3], perFrame[4], perFrame[5],
+        perFrame[6], perFrame[7] / 1000.0,
+        perFrame[8], perFrame[9] / 1000.0);
+      std::fflush(file);
+      frames = 0;
+      tick = now;
+    }
+  };
+
+  static DxvkFpsLog g_fpsLog;
+
+
   DxvkDevice::DxvkDevice(
     const Rc<DxvkInstance>&         instance,
     const Rc<DxvkAdapter>&          adapter,
@@ -210,6 +296,7 @@ namespace dxvk {
     
     std::lock_guard<sync::Spinlock> statLock(m_statLock);
     m_statCounters.addCtr(DxvkStatCounter::QueuePresentCount, 1);
+    g_fpsLog.frame(m_statCounters);
   }
 
 
@@ -224,6 +311,7 @@ namespace dxvk {
     m_submissionQueue.submit(submitInfo);
 
     std::lock_guard<sync::Spinlock> statLock(m_statLock);
+    m_lastSubmitted = commandList;
     m_statCounters.merge(commandList->statCounters());
     m_statCounters.addCtr(DxvkStatCounter::QueueSubmitCount, 1);
   }
@@ -241,7 +329,8 @@ namespace dxvk {
   }
 
 
-  void DxvkDevice::waitForResource(const Rc<DxvkResource>& resource, DxvkAccess access) {
+  void DxvkDevice::waitForResource(const Rc<DxvkResource>& resource, DxvkAccess access,
+                                   bool isImage) {
     if (resource->isInUse(access)) {
       auto t0 = dxvk::high_resolution_clock::now();
 
@@ -255,6 +344,10 @@ namespace dxvk {
       std::lock_guard<sync::Spinlock> lock(m_statLock);
       m_statCounters.addCtr(DxvkStatCounter::GpuSyncCount, 1);
       m_statCounters.addCtr(DxvkStatCounter::GpuSyncTicks, us.count());
+      if (isImage) {
+        m_statCounters.addCtr(DxvkStatCounter::GpuSyncImageCount, 1);
+        m_statCounters.addCtr(DxvkStatCounter::GpuSyncImageTicks, us.count());
+      }
     }
   }
   
diff --git a/src/dxvk/dxvk_device.h b/src/dxvk/dxvk_device.h
index e23f2c6..9b1c25b 100644
--- a/src/dxvk/dxvk_device.h
+++ b/src/dxvk/dxvk_device.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_adapter.h"
 #include "dxvk_buffer.h"
 #include "dxvk_compute.h"
@@ -411,6 +414,16 @@ namespace dxvk {
      * \param [in] waitSync (Optional) Semaphore to wait on
      * \param [in] wakeSync (Optional) Semaphore to notify
      */
+    /**
+     * \brief Most recently enqueued command list (may not be vkQueueSubmit'ed yet).
+     * For the small-render-target readback fast path: waiting on its fence directly
+     * skips the retirement thread's serialized per-submission fence waits.
+     */
+    Rc<DxvkCommandList> lastSubmittedList() {
+      std::lock_guard<sync::Spinlock> lock(m_statLock);
+      return m_lastSubmitted;
+    }
+
     void submitCommandList(
       const Rc<DxvkCommandList>&      commandList,
             VkSemaphore               waitSync,
@@ -474,7 +487,11 @@ namespace dxvk {
      * \param [in] resource Resource to wait for
      * \param [in] access Access mode to check
      */
-    void waitForResource(const Rc<DxvkResource>& resource, DxvkAccess access);
+    // isImage attributes the stall to a texture lock rather than a buffer lock. Both go
+    // through the same wait, and knowing which one dominates is the whole question when a
+    // frame is spending most of its time blocked here.
+    void waitForResource(const Rc<DxvkResource>& resource, DxvkAccess access,
+                         bool isImage = false);
     
     /**
      * \brief Waits until the device becomes idle
@@ -503,6 +520,7 @@ namespace dxvk {
 
     sync::Spinlock              m_statLock;
     DxvkStatCounters            m_statCounters;
+    Rc<DxvkCommandList>         m_lastSubmitted;
     
     DxvkDeviceQueueSet          m_queues;
     
diff --git a/src/dxvk/dxvk_extension_provider.h b/src/dxvk/dxvk_extension_provider.h
index 090d56d..cc6ff2d 100644
--- a/src/dxvk/dxvk_extension_provider.h
+++ b/src/dxvk/dxvk_extension_provider.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 #include "dxvk_extensions.h"
 
diff --git a/src/dxvk/dxvk_extensions.h b/src/dxvk/dxvk_extensions.h
index 0e2808d..8cc6d24 100644
--- a/src/dxvk/dxvk_extensions.h
+++ b/src/dxvk/dxvk_extensions.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <algorithm>
 #include <map>
 #include <vector>
diff --git a/src/dxvk/dxvk_fence.h b/src/dxvk/dxvk_fence.h
index 7974528..be790cb 100644
--- a/src/dxvk/dxvk_fence.h
+++ b/src/dxvk/dxvk_fence.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <functional>
 #include <queue>
 #include <utility>
diff --git a/src/dxvk/dxvk_framebuffer.h b/src/dxvk/dxvk_framebuffer.h
index b55e6e5..febe642 100644
--- a/src/dxvk/dxvk_framebuffer.h
+++ b/src/dxvk/dxvk_framebuffer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_image.h"
 #include "dxvk_renderpass.h"
 
diff --git a/src/dxvk/dxvk_gpu_event.h b/src/dxvk/dxvk_gpu_event.h
index d1f4d1b..01ab19d 100644
--- a/src/dxvk/dxvk_gpu_event.h
+++ b/src/dxvk/dxvk_gpu_event.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "dxvk_resource.h"
diff --git a/src/dxvk/dxvk_gpu_query.h b/src/dxvk/dxvk_gpu_query.h
index 0c45d12..2a9bb83 100644
--- a/src/dxvk/dxvk_gpu_query.h
+++ b/src/dxvk/dxvk_gpu_query.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_graphics.h b/src/dxvk/dxvk_graphics.h
index 50d16b1..68ee6bc 100644
--- a/src/dxvk/dxvk_graphics.h
+++ b/src/dxvk/dxvk_graphics.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 
 #include "../util/sync/sync_list.h"
diff --git a/src/dxvk/dxvk_graphics_state.h b/src/dxvk/dxvk_graphics_state.h
index b10f783..ee9e395 100644
--- a/src/dxvk/dxvk_graphics_state.h
+++ b/src/dxvk/dxvk_graphics_state.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_limits.h"
 
 #include <cstring>
diff --git a/src/dxvk/dxvk_hash.h b/src/dxvk/dxvk_hash.h
index a0fe561..27060d1 100644
--- a/src/dxvk/dxvk_hash.h
+++ b/src/dxvk/dxvk_hash.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <cstddef>
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_image.h b/src/dxvk/dxvk_image.h
index b3b8a58..401d284 100644
--- a/src/dxvk/dxvk_image.h
+++ b/src/dxvk/dxvk_image.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_descriptor.h"
 #include "dxvk_format.h"
 #include "dxvk_memory.h"
diff --git a/src/dxvk/dxvk_instance.h b/src/dxvk/dxvk_instance.h
index f945ed6..7abd483 100644
--- a/src/dxvk/dxvk_instance.h
+++ b/src/dxvk/dxvk_instance.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/config/config.h"
 
 #include "dxvk_adapter.h"
diff --git a/src/dxvk/dxvk_limits.h b/src/dxvk/dxvk_limits.h
index eb2431e..f86b70b 100644
--- a/src/dxvk/dxvk_limits.h
+++ b/src/dxvk/dxvk_limits.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_memory.h b/src/dxvk/dxvk_memory.h
index 795dd74..1377ec2 100644
--- a/src/dxvk/dxvk_memory.h
+++ b/src/dxvk/dxvk_memory.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_adapter.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_meta_blit.h b/src/dxvk/dxvk_meta_blit.h
index 28f8616..d24af49 100644
--- a/src/dxvk/dxvk_meta_blit.h
+++ b/src/dxvk/dxvk_meta_blit.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/dxvk/dxvk_meta_clear.h b/src/dxvk/dxvk_meta_clear.h
index eaf12ad..e38e43c 100644
--- a/src/dxvk/dxvk_meta_clear.h
+++ b/src/dxvk/dxvk_meta_clear.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_format.h"
 #include "dxvk_include.h"
 
diff --git a/src/dxvk/dxvk_meta_copy.h b/src/dxvk/dxvk_meta_copy.h
index c80b7f7..a9df5f1 100644
--- a/src/dxvk/dxvk_meta_copy.h
+++ b/src/dxvk/dxvk_meta_copy.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/dxvk/dxvk_meta_mipgen.h b/src/dxvk/dxvk_meta_mipgen.h
index a5d2d60..5fd94bf 100644
--- a/src/dxvk/dxvk_meta_mipgen.h
+++ b/src/dxvk/dxvk_meta_mipgen.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "dxvk_meta_blit.h"
diff --git a/src/dxvk/dxvk_meta_pack.h b/src/dxvk/dxvk_meta_pack.h
index 7c69d8c..c064f62 100644
--- a/src/dxvk/dxvk_meta_pack.h
+++ b/src/dxvk/dxvk_meta_pack.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../spirv/spirv_code_buffer.h"
 
 #include "dxvk_cmdlist.h"
diff --git a/src/dxvk/dxvk_meta_resolve.h b/src/dxvk/dxvk_meta_resolve.h
index 7216132..34662b9 100644
--- a/src/dxvk/dxvk_meta_resolve.h
+++ b/src/dxvk/dxvk_meta_resolve.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/dxvk/dxvk_openvr.h b/src/dxvk/dxvk_openvr.h
index a7d1042..15cb268 100644
--- a/src/dxvk/dxvk_openvr.h
+++ b/src/dxvk/dxvk_openvr.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_openxr.h b/src/dxvk/dxvk_openxr.h
index a368035..5fe44ee 100644
--- a/src/dxvk/dxvk_openxr.h
+++ b/src/dxvk/dxvk_openxr.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_options.h b/src/dxvk/dxvk_options.h
index 992ec76..d47a8ae 100644
--- a/src/dxvk/dxvk_options.h
+++ b/src/dxvk/dxvk_options.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../util/config/config.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_pipelayout.h b/src/dxvk/dxvk_pipelayout.h
index 9e96b34..64dd83f 100644
--- a/src/dxvk/dxvk_pipelayout.h
+++ b/src/dxvk/dxvk_pipelayout.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "dxvk_include.h"
diff --git a/src/dxvk/dxvk_pipemanager.h b/src/dxvk/dxvk_pipemanager.h
index 08c79e0..f93d67d 100644
--- a/src/dxvk/dxvk_pipemanager.h
+++ b/src/dxvk/dxvk_pipemanager.h
@@ -1,6 +1,9 @@
 
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <unordered_map>
 
diff --git a/src/dxvk/dxvk_platform_exts.h b/src/dxvk/dxvk_platform_exts.h
index f6f0306..eb34942 100644
--- a/src/dxvk/dxvk_platform_exts.h
+++ b/src/dxvk/dxvk_platform_exts.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_extension_provider.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_queue.cpp b/src/dxvk/dxvk_queue.cpp
index 86f0b4e..33ac50e 100644
--- a/src/dxvk/dxvk_queue.cpp
+++ b/src/dxvk/dxvk_queue.cpp
@@ -137,6 +137,34 @@ namespace dxvk {
   }
   
   
+  // DXVK_QUEUE_LOG=<path>: one line per second from the retirement thread — how many
+  // submissions retired, how much wall time their fence syncs took, and the worst single
+  // sync. Distinguishes "GPU is actually busy" from "completion notifications stack up",
+  // which is the open question behind the 26 ms visibility-readback stall (docs/MAX4K.md).
+  namespace {
+    struct QueueLog {
+      FILE* file = nullptr;
+      std::chrono::steady_clock::time_point tick = std::chrono::steady_clock::now();
+      uint64_t retired = 0, syncUs = 0, maxUs = 0, notifyUs = 0;
+      QueueLog() {
+        if (const char* p = std::getenv("DXVK_QUEUE_LOG"))
+          if (*p) file = std::fopen(p, "a");
+      }
+      void add(uint64_t sUs, uint64_t nUs) {
+        if (!file) return;
+        retired++; syncUs += sUs; notifyUs += nUs; if (sUs > maxUs) maxUs = sUs;
+        auto now = std::chrono::steady_clock::now();
+        if (now - tick >= std::chrono::seconds(1)) {
+          std::fprintf(file, "retired=%llu sync_ms=%.1f max_ms=%.1f notify_ms=%.1f\n",
+            (unsigned long long)retired, syncUs / 1000.0, maxUs / 1000.0, notifyUs / 1000.0);
+          std::fflush(file);
+          tick = now; retired = 0; syncUs = 0; maxUs = 0; notifyUs = 0;
+        }
+      }
+    };
+    QueueLog g_queueLog;
+  }
+
   void DxvkSubmissionQueue::finishCmdLists() {
     env::setThreadName("dxvk-queue");
 
@@ -162,8 +190,10 @@ namespace dxvk {
       
       VkResult status = m_lastError.load();
       
+      auto qlT0 = std::chrono::steady_clock::now();
       if (status != VK_ERROR_DEVICE_LOST)
         status = entry.submit.cmdList->synchronize();
+      auto qlT1 = std::chrono::steady_clock::now();
       
       if (status != VK_SUCCESS) {
         Logger::err(str::format("DxvkSubmissionQueue: Failed to sync fence: ", status));
@@ -175,6 +205,10 @@ namespace dxvk {
       // up any thread that's currently waiting on a resource in
       // order to reduce delays as much as possible.
       entry.submit.cmdList->notifyObjects();
+      auto qlT2 = std::chrono::steady_clock::now();
+      g_queueLog.add(
+        std::chrono::duration_cast<std::chrono::microseconds>(qlT1 - qlT0).count(),
+        std::chrono::duration_cast<std::chrono::microseconds>(qlT2 - qlT1).count());
 
       lock.lock();
       m_pending -= 1;
diff --git a/src/dxvk/dxvk_queue.h b/src/dxvk/dxvk_queue.h
index 374da55..4327561 100644
--- a/src/dxvk/dxvk_queue.h
+++ b/src/dxvk/dxvk_queue.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <condition_variable>
 #include <mutex>
 #include <queue>
diff --git a/src/dxvk/dxvk_recycler.h b/src/dxvk/dxvk_recycler.h
index b90befd..973351a 100644
--- a/src/dxvk/dxvk_recycler.h
+++ b/src/dxvk/dxvk_recycler.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 
diff --git a/src/dxvk/dxvk_renderpass.h b/src/dxvk/dxvk_renderpass.h
index 2dff878..455a5a9 100644
--- a/src/dxvk/dxvk_renderpass.h
+++ b/src/dxvk/dxvk_renderpass.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <mutex>
 #include <vector>
 #include <unordered_map>
diff --git a/src/dxvk/dxvk_resource.h b/src/dxvk/dxvk_resource.h
index 9a73ed8..d66b4ba 100644
--- a/src/dxvk/dxvk_resource.h
+++ b/src/dxvk/dxvk_resource.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_shader.h b/src/dxvk/dxvk_shader.h
index 0be8b49..75308bc 100644
--- a/src/dxvk/dxvk_shader.h
+++ b/src/dxvk/dxvk_shader.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "dxvk_include.h"
diff --git a/src/dxvk/dxvk_shader_key.h b/src/dxvk/dxvk_shader_key.h
index 4a66e52..1a48f83 100644
--- a/src/dxvk/dxvk_shader_key.h
+++ b/src/dxvk/dxvk_shader_key.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_hash.h"
 #include "dxvk_include.h"
 
diff --git a/src/dxvk/dxvk_signal.h b/src/dxvk/dxvk_signal.h
index fa99f78..99c1cf8 100644
--- a/src/dxvk/dxvk_signal.h
+++ b/src/dxvk/dxvk_signal.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk {
diff --git a/src/dxvk/dxvk_spec_const.h b/src/dxvk/dxvk_spec_const.h
index df4f041..b0662b0 100644
--- a/src/dxvk/dxvk_spec_const.h
+++ b/src/dxvk/dxvk_spec_const.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_limits.h"
 #include "dxvk_shader.h"
 
diff --git a/src/dxvk/dxvk_state_cache.h b/src/dxvk/dxvk_state_cache.h
index f23c85f..b14a571 100644
--- a/src/dxvk/dxvk_state_cache.h
+++ b/src/dxvk/dxvk_state_cache.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 #include <condition_variable>
 #include <fstream>
diff --git a/src/dxvk/dxvk_state_cache_types.h b/src/dxvk/dxvk_state_cache_types.h
index 41ff4d4..d8b4b9a 100644
--- a/src/dxvk/dxvk_state_cache_types.h
+++ b/src/dxvk/dxvk_state_cache_types.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_pipemanager.h"
 #include "dxvk_renderpass.h"
 
diff --git a/src/dxvk/dxvk_stats.h b/src/dxvk/dxvk_stats.h
index 9138c7c..8fcce93 100644
--- a/src/dxvk/dxvk_stats.h
+++ b/src/dxvk/dxvk_stats.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk {
@@ -22,6 +25,8 @@ namespace dxvk {
     QueuePresentCount,        ///< Number of present calls / frames
     GpuSyncCount,             ///< Number of GPU synchronizations
     GpuSyncTicks,             ///< Time spent waiting for GPU
+    GpuSyncImageCount,        ///< GPU synchronizations caused by locking an image
+    GpuSyncImageTicks,        ///< Time spent waiting for GPU on image locks
     GpuIdleTicks,             ///< GPU idle time in microseconds
     CsSyncCount,              ///< CS thread synchronizations
     CsSyncTicks,              ///< Time spent waiting on CS
diff --git a/src/dxvk/dxvk_swapchain_blitter.h b/src/dxvk/dxvk_swapchain_blitter.h
index 915bb53..2ca1918 100644
--- a/src/dxvk/dxvk_swapchain_blitter.h
+++ b/src/dxvk/dxvk_swapchain_blitter.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk/dxvk_device.h"
 #include "../dxvk/dxvk_context.h"
 
diff --git a/src/dxvk/dxvk_unbound.h b/src/dxvk/dxvk_unbound.h
index d9ae01e..685a9b8 100644
--- a/src/dxvk/dxvk_unbound.h
+++ b/src/dxvk/dxvk_unbound.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_buffer.h"
 #include "dxvk_image.h"
 #include "dxvk_sampler.h"
diff --git a/src/dxvk/dxvk_util.h b/src/dxvk/dxvk_util.h
index 94fd06d..afbf5d6 100644
--- a/src/dxvk/dxvk_util.h
+++ b/src/dxvk/dxvk_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "dxvk_include.h"
 
 namespace dxvk::util {
diff --git a/src/dxvk/hud/dxvk_hud.h b/src/dxvk/hud/dxvk_hud.h
index d6217b8..2e09291 100644
--- a/src/dxvk/hud/dxvk_hud.h
+++ b/src/dxvk/hud/dxvk_hud.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk_device.h"
 
 #include "dxvk_hud_item.h"
diff --git a/src/dxvk/hud/dxvk_hud_item.h b/src/dxvk/hud/dxvk_hud_item.h
index 275461c..d68d680 100644
--- a/src/dxvk/hud/dxvk_hud_item.h
+++ b/src/dxvk/hud/dxvk_hud_item.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <string>
 #include <unordered_map>
 #include <unordered_set>
diff --git a/src/dxvk/hud/dxvk_hud_renderer.h b/src/dxvk/hud/dxvk_hud_renderer.h
index 3986fba..cfc9b47 100644
--- a/src/dxvk/hud/dxvk_hud_renderer.h
+++ b/src/dxvk/hud/dxvk_hud_renderer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "../dxvk_device.h"
 
 #include "dxvk_hud_font.h"
diff --git a/src/spirv/spirv_code_buffer.h b/src/spirv/spirv_code_buffer.h
index a0ed227..3c44515 100644
--- a/src/spirv/spirv_code_buffer.h
+++ b/src/spirv/spirv_code_buffer.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <spirv/spirv.hpp>
 
 #include <iostream>
diff --git a/src/spirv/spirv_compression.h b/src/spirv/spirv_compression.h
index e48f398..4f2cb54 100644
--- a/src/spirv/spirv_compression.h
+++ b/src/spirv/spirv_compression.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "spirv_code_buffer.h"
diff --git a/src/spirv/spirv_instruction.h b/src/spirv/spirv_instruction.h
index ada816e..b53ba6d 100644
--- a/src/spirv/spirv_instruction.h
+++ b/src/spirv/spirv_instruction.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <spirv/spirv.hpp>
 #include <spirv/GLSL.std.450.hpp>
 
diff --git a/src/spirv/spirv_module.h b/src/spirv/spirv_module.h
index 7ce5b8f..b79b4f5 100644
--- a/src/spirv/spirv_module.h
+++ b/src/spirv/spirv_module.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <unordered_set>
 
 #include "spirv_code_buffer.h"
diff --git a/src/util/com/com_object.h b/src/util/com/com_object.h
index 75188d5..aadac88 100644
--- a/src/util/com/com_object.h
+++ b/src/util/com/com_object.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 
 #include "com_include.h"
diff --git a/src/util/config/config.h b/src/util/config/config.h
index f947272..bd1a4a0 100644
--- a/src/util/config/config.h
+++ b/src/util/config/config.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <string>
 #include <unordered_map>
 
diff --git a/src/util/log/log.h b/src/util/log/log.h
index 5d30162..d2a60fa 100644
--- a/src/util/log/log.h
+++ b/src/util/log/log.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <array>
 #include <fstream>
 #include <iostream>
diff --git a/src/util/rc/util_rc.h b/src/util/rc/util_rc.h
index c33d0c6..caee61b 100644
--- a/src/util/rc/util_rc.h
+++ b/src/util/rc/util_rc.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 
 namespace dxvk {
diff --git a/src/util/sha1/sha1.h b/src/util/sha1/sha1.h
index 06867b3..80caf78 100644
--- a/src/util/sha1/sha1.h
+++ b/src/util/sha1/sha1.h
@@ -11,6 +11,7 @@
 
 #include <stddef.h>
 #include <stdint.h>
+#include <stdint.h>
 
 #define	SHA1_BLOCK_LENGTH		64
 #define	SHA1_DIGEST_LENGTH		20
diff --git a/src/util/sha1/sha1_util.h b/src/util/sha1/sha1_util.h
index 9d83ab8..e9bc184 100644
--- a/src/util/sha1/sha1_util.h
+++ b/src/util/sha1/sha1_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <array>
 #include <cstring>
 #include <string>
diff --git a/src/util/sync/sync_recursive.h b/src/util/sync/sync_recursive.h
index 1bb64db..0689dcb 100644
--- a/src/util/sync/sync_recursive.h
+++ b/src/util/sync/sync_recursive.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 
 #include "../com/com_include.h"
diff --git a/src/util/sync/sync_signal.h b/src/util/sync/sync_signal.h
index cdbcae8..36a0316 100644
--- a/src/util/sync/sync_signal.h
+++ b/src/util/sync/sync_signal.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 #include <functional>
 #include <list>
diff --git a/src/util/sync/sync_spinlock.h b/src/util/sync/sync_spinlock.h
index 2715792..b16b911 100644
--- a/src/util/sync/sync_spinlock.h
+++ b/src/util/sync/sync_spinlock.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 
 #include "../thread.h"
diff --git a/src/util/sync/sync_ticketlock.h b/src/util/sync/sync_ticketlock.h
index 1c47f83..1845b35 100644
--- a/src/util/sync/sync_ticketlock.h
+++ b/src/util/sync/sync_ticketlock.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <atomic>
 
 #include "../thread.h"
diff --git a/src/util/thread.h b/src/util/thread.h
index bc58d22..e905545 100644
--- a/src/util/thread.h
+++ b/src/util/thread.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <chrono>
 #include <condition_variable>
 #include <functional>
diff --git a/src/util/util_bit.h b/src/util/util_bit.h
index 7682ace..5d3b5eb 100644
--- a/src/util/util_bit.h
+++ b/src/util/util_bit.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #ifndef _MSC_VER
 #if defined(__WINE__) && defined(__clang__)
 #pragma push_macro("_WIN32")
diff --git a/src/util/util_enum.h b/src/util/util_enum.h
index 85b9b21..c977da0 100644
--- a/src/util/util_enum.h
+++ b/src/util/util_enum.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #define ENUM_NAME(name) \
   case name: return os << #name
 
diff --git a/src/util/util_env.h b/src/util/util_env.h
index 80efcbc..8ccfcfa 100644
--- a/src/util/util_env.h
+++ b/src/util/util_env.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "util_string.h"
 
 namespace dxvk::env {
diff --git a/src/util/util_fps_limiter.h b/src/util/util_fps_limiter.h
index 9601dc9..58fdc23 100644
--- a/src/util/util_fps_limiter.h
+++ b/src/util/util_fps_limiter.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "thread.h"
 #include "util_time.h"
 
diff --git a/src/util/util_math.h b/src/util/util_math.h
index 5457ef0..a83745e 100644
--- a/src/util/util_math.h
+++ b/src/util/util_math.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <cmath>
 
 namespace dxvk {
diff --git a/src/util/util_matrix.h b/src/util/util_matrix.h
index 98f260f..d908eb8 100644
--- a/src/util/util_matrix.h
+++ b/src/util/util_matrix.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include "util_vector.h"
 
 namespace dxvk {
diff --git a/src/util/util_small_vector.h b/src/util/util_small_vector.h
index c313ca4..98d9d10 100644
--- a/src/util/util_small_vector.h
+++ b/src/util/util_small_vector.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <type_traits>
 
 namespace dxvk {
diff --git a/src/util/util_string.h b/src/util/util_string.h
index ae67403..6b9d332 100644
--- a/src/util/util_string.h
+++ b/src/util/util_string.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <cstring>
 #include <string>
 #include <sstream>
diff --git a/src/util/util_vector.h b/src/util/util_vector.h
index 77cdf29..cd1af57 100644
--- a/src/util/util_vector.h
+++ b/src/util/util_vector.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <iostream>
 
 #include "util_bit.h"
diff --git a/src/vulkan/vulkan_presenter.h b/src/vulkan/vulkan_presenter.h
index 40562f6..409f9a8 100644
--- a/src/vulkan/vulkan_presenter.h
+++ b/src/vulkan/vulkan_presenter.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <vector>
 
 #include "../util/log/log.h"
diff --git a/src/vulkan/vulkan_util.h b/src/vulkan/vulkan_util.h
index 38e0217..61cb692 100644
--- a/src/vulkan/vulkan_util.h
+++ b/src/vulkan/vulkan_util.h
@@ -1,5 +1,8 @@
 #pragma once
 
+#include <cstdint>
+#include <cstddef>
+
 #include <utility>
 
 #include "vulkan_loader.h"
